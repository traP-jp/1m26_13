# バッジ alpha 仕様

更新日: 2026-09-04
状態: 視覚生成規則は採用済み。Session完了からの獲得条件、画面全体の構成、本番実装技術は未決。

## 位置づけ

`references/badge-alpha.html`のソリッド幾何学紋章を、講習会バッジの最初の正式な表現として採用する。この文書はバッジの意味、生成規則、表示に必要な情報と品質条件を定める。参照HTMLのショーケース画面、使用ライブラリ、サンプルデータは本番仕様に含めない。

## バッジの意味

- バッジの視覚単位はLectureとし、そのLectureに対応するバッジを最大1個表示する。
- バッジ獲得を別状態として保存せず、Session完了記録から導出する。どのSession集合を満たしたら獲得するかは未決。
- バッジは修了証明や改ざん防止証明ではなく、プロフィール上で学習履歴を識別しやすくする視覚表現である。
- 年度が異なるLectureは別のLecture、別のバッジとして扱う。

## 生成入力と安定性

参照HTMLはLecture名をseedにしているが、本番では次の入力を正本にする。

```ts
type BadgeAlphaInput = {
  lectureId: LectureId;
  lectureName: string;
};
```

- 図形を決めるseedは`badge-alpha-v1:${lectureName}`とする。同じ講習会名なら年度や内部IDが異なっても同じ図形になる。
- `lectureName`は表示名として使い、図形のseedには使わない。同名Lectureを許容し、Lecture名を変更しても獲得済みバッジの外観を変えないためである。
- 同じ`lectureId`と生成器バージョンからは、端末、再読込、表示場所にかかわらず同じSVGを生成する。
- ハッシュ値は乱数初期化の内部値であり、本人確認、署名、権限判定には使わない。
- `badge-alpha-v1`の生成結果は固定する。将来アルゴリズムを変更するときは新しいバージョンを追加し、既存バージョンを黙って置換しない。

## 採用する視覚生成規則

- 出力は正方形のベクターSVGを正本とする。参照実装のviewBoxは`0 0 360 360`。
- 外周ベゼル、中間のソリッド図形、反転方向のアクセント、中心コアの4層で構成する。
- 太い輪郭、扇形、ブロック、シェブロン、グリッド、多角形を組み合わせたソリッド幾何学紋章とする。
- 構図は次の6系統をseedから決める。
  - `SEGMENTED RING`
  - `SOLID CREST`
  - `INTERLOCKING BLOCKS`
  - `RADIAL CHEVRONS`
  - `MODULAR GRID`
  - `HEAVY POLYGON`
- 回転対称数は`3`、`4`、`6`、`8`から決める。
- 色は参照実装の8パレットを初期セットとして固定する。
  - crimson: `#ef4444` / `#fca5a5` / `#ffffff`
  - cobalt: `#3b82f6` / `#93c5fd` / `#f59e0b`
  - emerald: `#10b981` / `#6ee7b7` / `#ffffff`
  - amber: `#f59e0b` / `#fde68a` / `#ef4444`
  - purple: `#8b5cf6` / `#c4b5fd` / `#06b6d4`
  - cyan: `#06b6d4` / `#a5f3fc` / `#f43f5e`
  - orange: `#ff5722` / `#ffcc80` / `#ffffff`
  - monochrome: `#f8fafc` / `#64748b` / `#38bdf8`
- 乱数列は参照実装の`cyrb53`とseeded RNGの結果を互換基準とする。ただし、正式実装時に同一seedのgolden SVGまたは同一パラメータをテストで固定する。

## 表示内容

プロフィールのバッジ一覧・詳細では、最低限次を扱う。

- バッジ画像
- Lecture名
- 学年度
- 獲得条件を満たした日、または完了したSessionの情報。最終表示は獲得条件と合わせて決める。
- 対応Lectureの詳細への導線

構図名、対称数、パレット名、内部ハッシュ、生成識別子、seedは通常の利用者画面には出さない。生成確認や不具合調査が必要な場合だけ、開発用情報として参照できるようにする。

## モーションと書き出し

- 中間図形の60秒回転とアクセントの40秒逆回転は、バッジ表現として利用できる任意の効果とする。
- `prefers-reduced-motion: reduce`では静止する。利用者がモーションを無効にした場合も静止する。
- 一覧で多数表示する場合は、可読性と描画負荷を確認して静止表示を選べる。
- 書き出し機能を設ける場合、SVGと1080×1080 PNGを静止画像として保存できるようにする。これはalphaの必須導線ではなく、画面設計で必要性を判断する。

## アクセシビリティ

- Lecture名が隣接して読まれる場合、装飾SVGは`aria-hidden="true"`にする。単独で表示する場合はLecture名を代替テキストとして与える。
- バッジを操作入口にする場合は、`button`または`a`を使い、キーボードとポインターで同じ操作ができるようにする。
- 詳細をダイアログで出す場合は、名前、`role="dialog"`、`aria-modal="true"`、初期フォーカス、Tab移動の閉じ込め、Escapeで閉じる操作、元の要素へのフォーカス復帰を備える。
- パレットの違いだけでLecture、完了、選択状態を伝えない。

## 参照HTMLから採用しないもの

- Tailwind CSS CDN、Lucide CDN、Google Fontsと、そのDOM・CSSを本番実装へコピーすること。
- 濃紺のページ背景、カードの列数、ヘッダー、モーダル、トーストなど、ショーケース画面全体の設計。
- 8件のサンプルLecture、タグ、説明文。
- 「全再描画」、seedコピー、内部ハッシュ、生成識別子、構図・対称数・パレットの常時表示。
- `div`のクリックだけで詳細を開く操作や、フォーカス管理のないモーダル。

## 画面設計で決めること

- プロフィール内での一覧密度、バッジ寸法、詳細の開き方。
- モーションを使う表示範囲と利用者向け切替の要否。
- SVG・PNG保存をalphaの初回実装へ含めるか。
- ロードマップ達成バッジを講習会バッジと同じ生成系にするか。
- Session完了から講習会バッジを獲得する条件と、詳細に表示する日付・Session情報。
- Lectureが削除・利用停止・非公開になった場合の獲得済みバッジ表示。

## 受け入れ条件

- 同じLecture IDから常に同じ構図、対称数、パレット、SVGが生成される。
- 同名の異なるLectureは別のバッジになり、Lecture名の変更では外観が変わらない。
- 確定した獲得条件に従い、Session完了の追加・取消がプロフィールのバッジ表示へ一貫して反映される。
- バッジ詳細にLecture名、学年度、確定後の獲得根拠、Lecture詳細への導線がある。
- モーション低減設定、キーボード操作、読み上げ、PC・スマートフォン幅を確認できる。

## 参照

- 原本: `/Users/soma/Downloads/workshop_badge_collection.html`
- リポジトリ内の固定参照: `references/badge-alpha.html`
- 原本と固定参照のSHA-256: `74c736cf1f33a7a5b78395cb864f4e2feb8061ef44a40fe7232194fd6fa681ff`
- 完了・バッジの上位仕様: `PRODUCTION_CONTEXT.md`
