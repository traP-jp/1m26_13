# Project instructions

日本語を既定とし、作業開始前に`PROJECT_BRIEF.md`、`PLAN.md`、`STATUS.md`、`DECISIONS.md`を読む。

## 作業原則

- `PROJECT_BRIEF.md`の目的、完了条件、初期スコープを達成するまで、承認不要の範囲では質問のために止まらず、合理的な仮定を置いて進める。
- ローカルで完結し、可逆で、無料で、目的とスコープを変えない判断は自律的に行ってよい。技術構成、実装順序、データモデルの詳細、UIの細部、テスト方法、旧デモからの移植範囲はこの範囲に含む。
- 無料かつ一般的なオープンソース依存関係は、目的達成に必要で、保守状況とライセンスを確認でき、外部アカウントや常時稼働サービスを必要としない場合に限り、承認なしで追加してよい。追加理由を`DECISIONS.md`へ記録し、lockfileを含めて検証する。
- 一度に扱う成果を小さくし、検証可能な単位で進める。
- 実施した変更、検証結果、現在のチェックポイント、次の一手を`STATUS.md`へ反映する。
- 重要な判断は`DECISIONS.md`へ追記する。判断理由、選択肢、決定、根拠、影響、再検討条件を残し、後から異なる判断をした場合は過去の記録を消さずに`superseded`としてつなぐ。
- 判断できる事項を`pending`のまま放置しない。確認必須の事項だけを`pending`として記録し、その他は決定して作業を続ける。
- 外部公開、有料サービスや購入、外部への送信・投稿、第三者への連絡、認証情報の投入、個人データの取り扱い変更、不可逆または破壊的な操作、目的やスコープの重大な変更は明示承認を求める。
- commit、push、Pull Request作成は明示依頼がある場合だけ行う。

## 長時間実行

- `PLAN.md`のチェックポイントを順に進め、各チェックポイントで実装、検証、記録まで完了させる。
- テストやビルドが失敗した場合は、原因を調査して修正し、再実行する。承認必須事項に当たらない限り、失敗報告だけで停止しない。
- 完了条件を満たすか、承認必須事項が真のブロッカーになるまで継続する。
- 完了時は、主要導線の動作確認、テストとビルド、既知の問題、残作業を`STATUS.md`へまとめる。

## ブラウザ調査

- `q.trap.jp`と`wiki.trap.jp`は、講習会の雰囲気、用語、既存運用、不明点を調べる目的で、ブラウザから閲覧・サイト内検索してよい。
- 両サイトは読み取り専用で扱う。投稿、編集、コメント、リアクション、投票、ファイル追加、設定変更、外部状態を変えるフォーム送信は行わない。
- 認証済みセッションを利用できる場合も閲覧限定とし、ログアウトやアカウント設定変更を行わない。
- 非公開情報を成果物へ不必要に転載しない。実装判断に使った場合は、必要最小限の要約、参照URL、判断への影響を`DECISIONS.md`へ記録する。

## 参照プロトタイプ

- 完了済みの講習会ウィザードデモ: `/Users/soma/Develop/workshop-wizard-demo`
- 参照プロトタイプは読み取り対象とし、明示依頼なしに変更しない。
- コードを一括コピーせず、全体プロダクトの設計に合う部分だけ、移植理由と検証方法を決めてから取り込む。

## Vaultとの接続

- Vault bridge: `/Users/soma/Documents/Obsidian/Codex/Projects/Active/1m26-13-alpha`
- `Handoff.md`と`Project.md`は参照用として扱う。
- VaultのDaily Noteを編集しない。
- `Checkpoint.md`は本人が進捗同期を依頼した場合だけ、短い要約を追記する。
- Vault側へ実装ログ、長い会話、機密情報を複製しない。
