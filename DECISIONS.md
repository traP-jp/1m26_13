# Decision Log

このファイルは、α版の実装中に必要になった重要な意思決定と、その理由を後から追跡できるように残す。

## 記録ルール

- ローカルで可逆な事項は、合理的に決定して`decided`として記録し、作業を続ける。
- 確認必須事項がブロッカーになった場合だけ`pending`を使用する。
- 決定を変更した場合は過去の記録を削除せず、旧記録を`superseded`にして新しい決定へリンクする。
- 単純な実装詳細をすべて記録するのではなく、別の担当者が「なぜこの形か」を知る必要がある判断を記録する。

## D-20260826-001 — 承認不要の範囲では自律的に完成まで進める

- 状態: decided
- 判断が必要だった理由: 長時間実行中に細かな確認待ちで停止せず、α版の完成まで継続できる運用が必要だった。
- 選択肢:
  1. 未決事項ごとに作業を止めて本人へ確認する。
  2. 承認境界を定義し、その内側では合理的な仮定を置いて自律的に進める。
- 決定: 選択肢2。ローカル、可逆、無料、目的とスコープの範囲内にある判断は自律的に行う。
- 根拠: α版では細部の最適化より、価値循環を端から端まで成立させて検証可能にすることを優先するため。
- 影響: 技術構成、データモデルの詳細、UIの細部、実装順序、必要な一般的OSS依存、旧デモの移植範囲について、原則として質問せず決定できる。
- 再検討する条件: 外部公開、費用、外部書き込み、個人データ、認証情報、破壊的操作、重大なスコープ変更が必要になった場合。
- 参照: `AGENTS.md`、`PROJECT_BRIEF.md`、`PLAN.md`

## D-20260826-002 — traP内の情報源はブラウザで読み取り専用利用する

- 状態: decided
- 判断が必要だった理由: 講習会の雰囲気、用語、既存運用を理解するため、内部情報源を調査に使う範囲を明確にする必要があった。
- 選択肢:
  1. ローカル資料だけで推測する。
  2. `q.trap.jp`と`wiki.trap.jp`を読み取り専用で閲覧・検索する。
- 決定: 選択肢2。ブラウザによる閲覧とサイト内検索に限定して利用する。
- 根拠: 文脈を確認しつつ、外部状態や第三者の情報へ不要な変更を加えないため。
- 影響: 投稿、編集、コメント、リアクション、投票、ファイル追加、設定変更などは行わない。実装判断に使った情報は必要最小限の要約とURLを本ログへ残す。
- 再検討する条件: 閲覧以外の操作が必要になった場合、または情報の取り扱いに不明点が生じた場合。
- 参照: `AGENTS.md`

## D-20260826-003 — α版の主要導線を6ステップに固定する

- 状態: decided
- 判断が必要だった理由: 広い構想の各領域を深掘りせず、最初に端から端まで検証できる一本道を定義する必要があった。
- 選択肢:
  1. 運営・学習者・推薦・共同編集をそれぞれ独立機能として広く作る。
  2. 登録から学習記録までを6ステップに限定してつなぐ。
- 決定: 選択肢2。①運営が基本情報を入力、②前提・後続を選んで登録、③学習者が検索・カテゴリで発見、④詳細から関係をたどる、⑤受講完了を記録、⑥プロフィールとバッジを確認、の6ステップとする。
- 根拠: `PROJECT_BRIEF.md`の最初のマイルストーンと、Vault記録の中心価値が一致しているため。
- 影響: 教材配信、ランキング、自動推薦、共同編集、汎用DSLは導線から外す。内部試用ではこの6ステップを主なフィードバック単位にする。
- 再検討する条件: 内部試用で、6ステップの途中に不可欠な作業が欠けていると判明した場合。
- 参照: `PROJECT_BRIEF.md`、`PLAN.md`、Vault `Handoff.md` / `Project.md`

## D-20260826-004 — Sites + D1を基盤とし、basiQ-uiのVue UIを統合する

- 状態: decided
- 判断が必要だった理由: ローカルで再現でき、構造化データを永続化できる基盤と、利用するUIライブラリを確定する必要があった。
- 選択肢:
  1. 旧デモのGo/Vue構成をそのまま拡張する。
  2. SitesのVinext/D1基盤へVueのUI islandを載せ、`basiq-ui`を利用する。
  3. Reactだけで作り、`basiq-ui`の見た目だけを模倣する。
- 決定: 選択肢2。API・D1接続・配信はSites/Vinext、利用者向けUIはVue 3と`basiq-ui@0.1.0-beta.0`で実装する。
- 根拠: D1によりローカルと将来のSites環境で同じ永続化モデルを使え、本人指定の`basiq-ui`はVue 3をpeer dependencyとするため。`basiq-ui`はMIT、依存する`reka-ui`もMITであることを確認した。
- 影響: `.openai/hosting.json`はD1 binding `DB`を持つ。`basiq-ui`は公開直後のパッケージを制限する環境で通常解決できなかったため、参照デモで使用・integrity確認済みの同一成果物を`app/vendor`へライセンス込みで固定し、lockfileから再現できるようにする。外部公開は行わない。
- 再検討する条件: Vue islandとVinextの組み合わせでビルドまたはアクセシビリティ上の解消不能な問題が発生した場合、または`basiq-ui`の安定版・React対応版が提供された場合。
- 参照: `app/package.json`、`app/package-lock.json`、`app/.openai/hosting.json`、参照デモ `web/package.json`

## D-20260826-005 — 認証なしの固定試用ユーザーと6テーブルで始める

- 状態: superseded（固定試用ユーザーは継続、6テーブル構成はD-20260902-011/D-20260902-013で置換）
- 判断が必要だった理由: 個人データや外部認証を扱わずに、講習会・関係・受講履歴・バッジの整合性を検証できる最小モデルが必要だった。
- 選択肢:
  1. 本人認証と複数ユーザー管理まで実装する。
  2. 非個人情報の固定試用ユーザーをseedし、認証はα版の外とする。
- 決定: 選択肢2。`users`、`workshops`、`workshop_relations`、`completions`、`badges`、`user_badges`の6テーブルをD1へ永続化する。関係は「前提→後続」の有向辺とし、自己参照・重複・循環を拒否する。
- 根拠: 認証方式と個人データの取り扱いは明示承認が必要だが、固定personaなら主要な価値循環を安全に検証できるため。
- 影響: UIには内部試用の固定personaであり認証ではないことを示す。IDは安定した文字列、日時はUTC ISO 8601文字列を使う。登録済み講習会は即時に一覧へ出す。
- 再検討する条件: α試用後に認証方式と個人データ方針が承認された場合。
- 参照: `PROJECT_BRIEF.md`の制約、`app/db/schema.ts`

## D-20260826-006 — 検索・バッジ・旧デモ移植を最小仕様に限定する

- 状態: superseded（D-20260902-013/D-20260902-015で置換）
- 判断が必要だった理由: 探索、達成感、旧デモ再利用を、α版の価値検証に必要な深さへ絞る必要があった。
- 選択肢:
  1. FTS、複数バッジ、Markdown DSL Parserを初回から組み込む。
  2. 部分一致とカテゴリ、初回完了バッジ1個、固定フォームで始める。
- 決定: 選択肢2。検索はタイトル・概要・説明の部分一致とカテゴリ完全一致、バッジは完了1件以上で得る「はじめの一歩」1個とする。完了取消で0件になればバッジも取り消す。旧デモからはRenderer分離、入力保持、エラー表示、テスト観点を採り入れ、Parser/Run/CompletionViewのコードは移植しない。
- 根拠: 登録フォームの項目が固定された現段階ではDSLの複雑さが価値循環の検証に寄与せず、検索とバッジも最小規則の方が一貫性を自動テストしやすいため。
- 影響: 完了登録は冪等、プロフィールの履歴とバッジは常に整合させる。旧デモ本体は読み取り専用のまま保持する。
- 再検討する条件: 内部試用で、入力定義の頻繁な変更、検索精度不足、段階的達成の必要性が確認された場合。
- 参照: 参照デモ監査、`PROJECT_BRIEF.md`の「今はやらないこと」

## D-20260826-007 — 自動テストとブラウザ一巡をリリースゲートにする

- 状態: decided
- 判断が必要だった理由: 各画面が個別に表示できるだけでなく、永続化を含む一本道が壊れていないことを再現可能に保証する必要があった。
- 選択肢:
  1. 手動確認だけで停止条件を判定する。
  2. ドメイン/APIテスト、型検査、lint、buildに加え、主要導線のブラウザE2Eを必須にする。
- 決定: 選択肢2。空DBへの初期化、再読込、検索、関係制約、完了の冪等性、バッジ整合性、入力エラー、主要6ステップを自動・ブラウザ双方で検証する。
- 根拠: D1、API、Vue UIの境界をまたぐ不具合は単一層のテストだけでは検出できないため。
- 影響: 中核シナリオのskipやリトライ依存を残さない。全ゲートが成功してからチェックポイント6を完了とする。
- 再検討する条件: 実行環境の制約でブラウザ自動化が不可能な場合。ただし代替の実ブラウザ操作記録を必須とする。
- 参照: `PLAN.md`停止条件

## D-20260826-008 — migrationを起動時スキーマの正本にし、統合テストを隔離する

- 状態: decided
- 判断が必要だった理由: 手書きDDLとDrizzle migrationのずれ、および共有D1を使うテストによる試用データ破壊を防ぎ、新規環境と再起動後の永続化を再現可能に検証する必要があった。
- 選択肢:
  1. 起動用DDL、Drizzle schema、migrationを別々に保守し、既存の開発DBでAPIテストする。
  2. checked-in migrationを起動時にも適用し、適用台帳を持たせ、テストごとに一時D1を起動する。
- 決定: 選択肢2。`drizzle/0000_red_sleepwalker.sql`を`db/migrations.ts`から読み込み、`alpha_schema_migrations`で適用を管理する。API統合テストは一時ディレクトリのD1でサーバーを起動・再起動し、終了時にその一時領域だけを破棄する。
- 根拠: migrationと実際の初期化経路を一致させ、普段の`.wrangler`状態に触れずに空DB、再読込、関係のrollback、完了・バッジ境界を検証できるため。
- 影響: `npm run test:api`単体で主要API導線を再現できる。関係は前提・後続合計20件、ID 120文字に制限し、循環判定は非再帰化した。並行完了時の新規バッジ応答は`INSERT ... RETURNING`で実際に挿入した行だけを返す。
- 再検討する条件: migrationが複数になったときは、新しいSQLを適用リストへ追加し、既存DBからの段階移行を統合テストへ加える。
- 参照: `app/db/migrations.ts`、`app/db/setup.ts`、`app/tests/api.integration.test.mjs`

## D-20260826-009 — 公開経過期間を守りつつ依存脆弱性を解消する

- 状態: decided
- 判断が必要だった理由: scaffold時点のNext/Vinext/Vite/Cloudflare依存にhighの既知脆弱性があり、一方で公開直後3日未満の最新版は採用しない制約があった。
- 選択肢:
  1. scaffoldの固定版を維持し、既知問題をそのまま受け入れる。
  2. 互換範囲内かつ公開後3日以上の修正版へ更新し、Nextの推移依存だけ安全版へoverrideする。
  3. 公開当日の最新版へ全面更新する。
- 決定: 選択肢2。Nextは公開経過期間を満たす`16.2.12`、React系は`19.2.8`、Vinextは`1.0.0-beta.8`、Viteは`8.2.2`、Cloudflare pluginは`1.53.1`へ揃え、PostCSSとSharpを監査済み版へoverrideした。
- 根拠: peer dependencyを満たしたままbuild・統合テストが成功し、`npm audit --omit=dev`を0件にできたため。
- 影響: 開発専用`drizzle-kit@0.31.10`には古いesbuild-loader由来のmoderate 4件が残る。監査が示す唯一の自動修正は非互換な`0.18.1`へのdowngradeなので採用せず、migration生成は信頼できるローカルschemaだけに限定する。
- 再検討する条件: 現行Drizzle schemaと互換な修正版が公開後3日を経過した場合。
- 参照: `app/package.json`、`app/package-lock.json`、`STATUS.md`

## D-20260826-010 — α版の操作文脈はURLとタブ内下書きで保持する

- 状態: decided
- 判断が必要だった理由: 検索から詳細へ移ると条件が消え、登録中の再読み込みで長文入力が失われると、主要導線の試用が中断されるため。
- 選択肢:
  1. 画面コンポーネントのメモリだけで状態を保持する。
  2. 検索条件はURL、登録下書きは`sessionStorage`へ保存し、個人情報を扱う永続アカウント保存は行わない。
- 決定: 選択肢2。検索・カテゴリをquery parameterへ同期し、詳細の戻る導線へ引き継ぐ。登録下書きは同じタブのsession中だけ復元し、登録成功または明示破棄で消す。
- 根拠: 外部サービスや個人データを増やさず、誤操作・再読み込みから回復できるため。
- 影響: SPA遷移時の`document.title`、mainフォーカス、`aria-current`、フィールドエラー関連付け、モバイル主要ナビも同時に整備した。OGカードはローカル用メタデータへ設定したが、外部公開はしていない。
- 再検討する条件: 複数端末・複数利用者の認証と下書き同期方針が承認された場合。
- 参照: `app/ui/AlphaApp.ts`、`app/ui/views/WorkshopListView.ts`、`app/ui/views/WorkshopWizardView.ts`、`app/public/og.png`

## D-20260902-011 — 講習会と開催を分離し、講習会を連番IDで識別する

- 状態: decided
- 判断が必要だった理由: α版の1テーブル1講習会では、複数回、再放送、年度ごとの独立教材を一つの資産として扱えない。
- 選択肢:
  1. α版の文字列ID付き講習会を開催として流用する。
  2. 連番INTEGER（SQLite上の64-bit signed integer）を持つ講習会と、その下の連番開催へ分離する。
- 決定: 選択肢2。URLは`/workshops/{id}`、人が理解する回は`#1`、`#2`で表し、開催の内部IDはURLフラグメントに露出しない。
- 根拠: 記入済み担当Aの決定と今回の明示指示に一致し、1回開催では階層を隠しながら複数回を同じページへまとめられる。
- 影響: αテーブルと利用者のローカルデータは破壊せず、追加migrationでβテーブルを作る。検索、完了、ロードマップは講習会、日時・教材・対象・班・年度・公開状態は開催を参照する。
- 再検討する条件: 本番DBがSQLite/D1以外になり、BIGINTの表現方法を変える必要が生じた場合。
- 参照: 記入済み詳細設計A1、今回依頼

## D-20260902-012 — 開催の複製は値コピー後に完全独立させる

- 状態: decided
- 判断が必要だった理由: 再放送・次回を素早く作りつつ、後の変更を元開催へ波及させないルールが必要だった。
- 選択肢:
  1. 共通情報を参照し続け自動同期する。
  2. 利用者入力項目を全コピーし、種別とコピー元だけを関係として保持する。
- 決定: 選択肢2。複製後は通常編集ですべて変更でき、同期しない。完了・参加者・履歴などのシステム記録はコピーしない。
- 根拠: 担当Aの確定方向であり、過去資産を安全な出発点として再利用できるため。
- 影響: コピー元削除は実装しない。コピー元参照は任意外部キーとして残し、通常回番号は非再放送の開催だけで採番する。
- 再検討する条件: 一括更新の具体的な利用例と必要性が内部試用で確認された場合。
- 参照: 記入済み詳細設計A1、今回依頼

## D-20260902-013 — βの公開・完了・バッジ・ロードマップ状態を重複保存しない

- 状態: decided
- 判断が必要だった理由: 記入済み資料に未決・矛盾があり、中心体験を一貫させる最小規則が必要だった。
- 選択肢:
  1. 開催状況、バッジ、ロードマップ進捗を別々に保存する。
  2. 開催は`draft/published`だけを保存し、予定/過去は日時、バッジとロードマップ進捗は講習会完了から導出する。
- 決定: 選択肢2。講習会は公開開催を1件以上含むとき発見可能。完了は本人による講習会単位のトグルで、開催方法は区別しない。完了した公開講習会ごとにバッジを表示する。
- 根拠: 詳細設計A2/E1〜E4の多数意見と「過去の講習会を資産にする」を最少の状態で満たす。
- 影響: 下書きへ戻しても完了事実は保持するが、学習者向け一覧・詳細・プロフィール表示からは公開開催がない講習会を隠す。削除はβスコープ外。
- 再検討する条件: 権限、監査履歴、総集編による部分完了が具体仕様として確定した場合。
- 参照: 記入済み詳細設計A2、E1〜E4

## D-20260902-014 — βは既存依存だけで実装し、basiQ-uiの実部品を操作の正本にする

- 状態: decided
- 判断が必要だった理由: UI品質を上げつつ、依存追加と独自デザインシステム化を避ける必要があった。
- 選択肢:
  1. 新しいUI/フォームライブラリを追加する。
  2. 導入済み`basiq-ui`のThemeProvider、Button、Card、ChoiceGroup、ChoiceGroupItem、ToggleButtonを優先し、不足する入力レイアウトだけ共通CSSにする。
- 決定: 選択肢2。新規依存は追加しない。
- 根拠: パッケージのREADME、型定義、実装済み利用箇所を監査し、βに必要な選択・操作部品を満たせると判断した。
- 影響: PCと390px、キーボード、フォーカス、通常/読込/0件/失敗/保存中/保存失敗をリリースゲートにする。
- 再検討する条件: basiQ-uiにない複雑な部品が中心体験で必要になった場合。
- 参照: `app/node_modules/basiq-ui/README.md`、`app/node_modules/basiq-ui/dist/index.d.ts`

## D-20260902-015 — αの挙動知見だけをkeepし、UI・データ境界をβとして再設計する

- 状態: decided
- 判断が必要だった理由: α版の画面、情報設計、文言、巨大CSS、コンポーネント構造を継承すると、記入済み設計と内部ツールとしての品質を満たせないという評価が確定した。
- 選択肢:
  1. αの画面へβ機能を継ぎ足す。
  2. keep / rewrite / removeを明示し、動作知見を使ってβのUI・型・データ境界を再設計する。
- 決定: 選択肢2。
  - keep: Vinext/D1のローカル基盤、migration適用、固定試用persona、入力保持・URL状態・フォーカス管理の観点、隔離D1/API統合テストの観点。
  - rewrite: 講習会/開催/ロードマップの型とAPI境界、repository、Vueのアプリシェル・全主要ビュー・共有開催フォーム、情報設計、文言、CSS。
  - remove: `AlphaApp`、1,034行の単一`globals.css`、カテゴリ中心の探索、件数バッジを目的化した表示、αの文字列講習会IDと1行1講習会をβ画面へ流す経路。αテーブル自体は利用者データ保護のため削除しない。
- 根拠: 記入済み設計と今回の品質条件では、過去教材を探して使う行動、単発時の階層非表示、複数回/再放送の同一ページ閲覧が中心であり、αの表示構造とは異なるため。
- 影響: CSSはbasiQ-ui本体と`base/layout/forms/pages`へ分け、ThemeProvider、Button、Card、ChoiceGroup/Item、ToggleButtonを実部品として使用する。カード、角丸、装飾統計は情報の境界または操作上必要な箇所だけに限定する。旧D-20260826-006の検索・バッジ・固定フォーム仕様は置換する。
- 再検討する条件: 内部試用で、現在の情報密度や画面境界が実際の運用に合わない具体例が得られた場合。
- 参照: 今回の重要方針修正、`app/ui/BetaApp.ts`、`app/styles/`

## D-20260902-016 — 内部サイトは今回の実装判断に使用しない

- 状態: superseded（本人が指定したtraQチャンネルの読み取り専用レビューをD-20260902-020で許可・採用）
- 判断が必要だった理由: `q.trap.jp`と`wiki.trap.jp`は読み取り専用で参照可能だったが、記入済み詳細設計とローカル資料だけでβ中心体験を確定できた。
- 選択肢:
  1. 認証済み内部サイトを追加調査する。
  2. 必要性のない非公開情報へのアクセスを避ける。
- 決定: 選択肢2。両サイトは閲覧せず、コード・文書へ非公開情報を取り込んでいない。
- 根拠: 今回の設計判断に不足がなく、必要最小限の情報利用に留めるため。
- 影響: 内部サイト由来のURL・内容・引用は成果物に含まれない。
- 再検討する条件: 実運用用語についてローカル資料だけでは判断できない具体的な疑問が生じた場合。
- 参照: `AGENTS.md`、今回依頼

## D-20260902-017 — 生成画像は情報設計の方向として使い、架空機能は実装しない

- 状態: superseded（条件検索の比較行だけD-20260902-018でカード表示へ変更。その他の判断は継続）
- 判断が必要だった理由: `BETA_DESIGN_QUESTIONS.md`等に貼られた生成画像は現行βより明確な画面階層を示す一方、未実装のカテゴリ、作者、共有、推薦、編集機能やサンプル値も含み、そのまま仕様にすると未接続モックが増える。
- 選択肢:
  1. 生成画像を画面仕様として忠実に複製し、不足データは仮値で補う。
  2. 画像群に共通する情報密度、ナビゲーション、列構成、状態色だけを採用し、文言・項目・操作は実APIと記入済み設計へ合わせる。
- 決定: 選択肢2。PCは約240pxの薄灰左ナビと白い作業面、発見はカード壁でなく比較行、詳細/ロードマップ/学びの記録は主列＋右レールとする。390pxでは左ナビを上部ブランド＋固定下部ナビへ変換し、絞り込みを`details`で畳み、右レールを主内容より先に縦積みする。
- 根拠: 9枚の画像、Questions、Worksheet、生成プロンプトを照合すると、この骨格は一貫している。一方で各画像は議論用であり、現在のD1/APIにない表示を足す根拠にはならない。
- 影響: `/`を実データのホームとして分離し、`/workshops?view=roadmaps`を実切替可能な発見面にした。basiQ-uiの公開6部品は用途に合う箇所で継続し、ライブラリに存在しないナビ、タブ、入力、一覧、進捗はsemantic HTMLとbasiQトークンで実装した。新規依存は追加していない。成功色だけはbasiQにトークンがないため、完了状態専用の最小共通変数を追加した。
- 再検討する条件: 内部試用で情報密度、モバイル下部ナビ、詳細レールの順序に具体的な支障が確認された場合、またはbasiQ-uiに対応部品・成功色tokenが追加された場合。
- 参照: Vault `BETA_DESIGN_QUESTIONS.md`、`BETA_DESIGN_WORKSHEET.md`、`IMAGE_GENERATION_PROMPTS.md`、`assets/beta-design/*.png`、`app/ui/`、`app/styles/`

## D-20260902-018 — 条件検索結果は密度を保ったBasiqCardで表示する

- 状態: decided
- 判断が必要だった理由: 比較行として実装した講習会検索結果について、本人からカード表示を試す明示依頼があった。
- 選択肢:
  1. D-20260902-017の比較行を維持する。
  2. 実データの比較項目を残しつつ、実際の`BasiqCard`で各講習会を一つのリンクカードにする。
- 決定: 選択肢2。タイトル、概要、班、年度、開催数だけを載せ、カード全体を詳細リンクにする。PCは3列、1050px以下は2列、760px以下は1列にする。カード本体の背景、境界、hoverは上書きせず、`BasiqCard`の標準props、slot、surface表現をそのまま使う。
- 根拠: 今回の明示依頼を小さく可逆なUI実験として反映でき、架空項目や装飾を増やさずカードの見つけやすさを評価できるため。
- 影響: D-20260902-017の「条件検索を比較行にする」部分だけを置換する。ロードマップ一覧、ホームの新着、詳細画面の情報構造は変更しない。フォーカスはカードを包む単一リンクに置き、読み上げ順は見出し、概要、班、年度、開催数、詳細の順とする。
- 再検討する条件: 講習会数が増えた内部試用で一覧性や走査速度が比較行より悪いと確認された場合。
- 参照: 今回依頼、`app/ui/views/WorkshopListView.ts`、`app/styles/pages.css`

## D-20260902-019 — 発見入口を分け、ホームと検索で班マーク付き講習会カードを共有する

- 状態: decided
- 判断が必要だった理由: ホームに検索方法と登録方法を大きく並べた結果、学習者の主目的である講習会の発見より運営導線が強く、同じ講習会もホームでは行、検索ではカードという不一致があった。また「学びの記録」より利用者ページ全体を表す名称が必要だった。
- 選択肢:
  1. ホームの大きな条件検索/ロードマップ検索/登録パネルと、画面内の探し方タブを維持する。
  2. 条件検索とロードマップを共通ナビから直接開く独立入口にし、ホームは最近の講習会と検索導線へ絞り、登録は共通ナビの運営操作だけにする。
- 決定: 選択肢2。「学びの記録」は「プロフィール」へ改称する。ホームと条件検索は同じ共有`WorkshopSummaryCard`を使い、実際の`BasiqCard`のheader/body/footerへタイトル、概要、班、年度、開催数を配置する。名称左は汎用教材アイコンでなく、最初の担当班を`Web`、`Sys`、`アル`のように短縮した班マークにする。カード本体のsurface、境界、hoverは独自CSSで変更しない。
- 根拠: 共通ナビだけで検索、ロードマップ、プロフィール、登録へ到達でき、ホームでは実データの講習会が主役になる。班の正式名称を本文にも残すため、短縮マークは装飾的な識別補助に限定できる。
- 影響: D-20260902-017のホームに2種類の発見入口を置く判断と、D-20260902-018のホーム新着を変更しない判断をこの範囲で置換する。`/workshops?view=roadmaps`とロードマップ機能自体は維持するが、「ロードマップから探す」という画面内切替は削除する。新規依存はない。
- 再検討する条件: 担当班が複数ある講習会で単一マークが誤解を生む、または公式の班アイコンセットがbasiQ-ui/traPデザイン資産として提供された場合。
- 参照: 今回依頼、`app/ui/components/WorkshopSummaryCard.ts`、`app/ui/views/HomeView.ts`、`app/ui/views/WorkshopListView.ts`、`app/ui/BetaApp.ts`

## D-20260902-020 — ホームを検索の起点にし、作成・管理操作を運営向けページへ集約する

- 状態: decided
- 判断が必要だった理由: 指定された内部レビューでは、ホームから全講習会を同じページで見たい、検索バーをホームへ置きたい、検索専用ナビを減らしたい、学習者向けロードマップ閲覧と運営の管理操作を分けたい、という利用行動が一貫して示された。
- 選択肢:
  1. ホーム4件＋「すべて見る」、検索専用ナビ、個別の作成導線、閲覧専用ロードマップを維持する。
  2. ホームへ検索バーと公開全件を置き、運営向けページへ講習会作成とロードマップ管理を集約し、ロードマップCRUDをD1/APIへ接続する。
- 決定: 選択肢2。学習者ナビはホーム、ロードマップ、プロフィールに絞る。ホーム検索は既存`/workshops?q=`へ接続する。`/admin`を運営の入口とし、ロードマップは閲覧URLと管理URLを分け、段階・講習会順・公開状態を作成/編集/削除できるようにする。
- 根拠: ホームで検索と一覧走査が完結し、運営操作は役割別にまとまる。ロードマップは既存のD1テーブルをそのまま正本にでき、未接続モックを増やさず実装できる。
- 影響: D-20260902-019の「共通ナビから条件検索を直接開く」と「ホームは最近の講習会」という部分を置換する。ロードマップ削除は関連する段階/項目をcascade削除するため編集画面で明示確認する。認証未実装のローカルβであることは継続し、本番権限設計は別途必要。
- 再検討する条件: 公開承認、共同編集、段階/講習会のドラッグ並べ替え、監査履歴が内部試用で必要と確認された場合。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/HomeView.ts`、`app/ui/views/AdminHomeView.ts`、`app/ui/views/RoadmapEditorView.ts`、`app/app/api/roadmaps/`

## D-20260902-021 — 新規作成は案内付きへ統一し、プロフィールを3タブとして完成させる

- 状態: decided（プロフィールでbasiQ-uiの`Button`外観を使う部分はD-20260902-027、新規作成で最初の開催まで3段階入力する部分はD-20260902-028でsuperseded。作成入口の統一とARIA/キーボード操作は継続）
- 判断が必要だった理由: 追加の内部レビューで、新規作成時に「案内付き」と「通常フォーム」を選ばせる必要性が薄く、プロフィールの3領域もタブとして明確に扱うことが求められた。
- 選択肢:
  1. 新規作成の二経路と、現状のプロフィール表示を維持する。
  2. 新規作成を案内付きウィザードへ統一し、既存講習会の全項目編集だけを一画面の通常編集として残す。プロフィールは3タブのARIA/キーボード操作を完成させる。
- 決定: 選択肢2。`/admin/workshops/new?mode=form`への導線と入力方法タブを削除し、旧URLへ直接アクセスしても同じ案内付き画面を表示する。プロフィールは「バッジ」「完了した講習会」「ロードマップ」の3タブを維持し、重複説明を削除する。
- 根拠: 初回登録は情報単位を順番に確認できる一経路へ絞るほうが迷いがなく、複製後を含む既存データの全項目変更は通常編集画面で引き続き満たせる。タブは既存のbasiQ-uiボタンを使い、独自の外観CSSを追加せず操作意味だけを強化できる。
- 影響: D-20260902-020の「案内付き/通常フォームを運営向けページへ集約する」部分を置換する。保存API、`WorkshopInput`、下書き復元、編集・複製、D1モデルは変更しない。
- 再検討する条件: 内部試用で、熟練運営者の新規一括入力が明確な時間短縮になる具体例が得られた場合。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/ProfileView.ts`、`app/ui/views/AdminHomeView.ts`、`app/ui/views/WorkshopWizardView.ts`

## D-20260902-022 — 教材導線を本文へ一本化し、通常開催を無標示の既定状態にする

- 状態: decided
- 判断が必要だった理由: 追加の内部レビューで、本文と学習状況カードに同じ教材リンクが重複し、通常開催にも毎回種別ラベルが付くことで詳細画面の情報量が増えていると確認された。
- 選択肢:
  1. 重要操作として教材リンクを2か所に置き、全開催へ種別ラベルを表示する。
  2. 教材リンクは本文の「教材」節へ一本化し、通常開催はラベルなし、再放送・総集編だけを例外ラベルで示す。
- 決定: 選択肢2。同じ回の通常開催と再放送は、再放送側だけのラベルと教材補助文で区別する。保存済みの`kind`と`copiedFromOccurrenceId`は変更しない。
- 根拠: 教材の正本となる表示場所が一つになり、通常状態の反復表示を減らせる一方、担当Aで確定した「通常開催と再放送を回の中で区別する」要件は例外側の明示で満たせる。
- 影響: 学習状況カードは完了状態と完了操作だけを扱う。教材未登録状態は本文の「教材」節だけで表示する。新規CSSと依存は追加せず、通常ラベル用の未使用スタイルを削除する。
- 再検討する条件: 教材への主要導線が見つからないという実利用の観察、または通常以外の開催種別が増えて凡例が必要になった場合。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/WorkshopDetailView.ts`

## D-20260902-023 — ロードマップの段階文言を廃止し、公開状態をbooleanへ移行する

- 状態: decided
- 判断が必要だった理由: 追加の内部レビューで、段階名・説明は学習者が講習会を選ぶうえで冗長であり、公開状態も文字列二値ではなくbooleanとして保持する方針が示された。既存試用D1を破壊せず移行する方法も必要だった。
- 選択肢:
  1. 段階名・説明と`draft/published`文字列を維持し、表示だけ減らす。
  2. 段階名・説明を入力型・API・表示から外し、講習会を直接並べる。公開状態の正本を`published INTEGER`へ移し、旧列は互換用途だけに残す。
- 決定: 選択肢2。段階は講習会の順序を保持する内部のまとまりとして残すが、名称・説明を意味データとして扱わない。新規保存では旧`title/description`へ空値を書き、公開判定は常にboolean列を読む。旧`status`列は既存テーブルの`NOT NULL`制約を壊さないため同値を影書きするが、アプリケーションの読取元にはしない。
- 根拠: 講習会名へ視線を直接誘導でき、APIとUIの不要フィールドを同時に除去できる。追加カラムとbackfillだけのmigrationなら、既存のロードマップ、講習会、完了記録を削除せず移行できる。
- 影響: `RoadmapInput.published`は厳密なbooleanとして検証する。学習者画面は全段階の項目を順番どおり平坦化し、basiQ-uiの`ToggleButton`が管理画面の公開操作になる。新規依存・独自UI部品・新規CSSセレクタは追加しない。
- 再検討する条件: 段階ごとの到達目標や公開範囲が、実運用上の必須情報として具体例とともに確認された場合、または本番移行時に互換列を削除できるDB保守時間が確保された場合。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/drizzle/0002_roadmap_published_boolean.sql`、`app/ui/views/RoadmapDetailView.ts`、`app/ui/views/RoadmapEditorView.ts`

## D-20260902-024 — ロードマップ管理一覧の操作と公開状態を部品・色で区別する

- 状態: decided
- 判断が必要だった理由: push後の再確認で、一覧の文字リンクでは閲覧・編集が操作に見えにくく、公開状態と非公開状態の識別も弱いという追加レビューがあった。
- 選択肢:
  1. 文字リンクと無彩色の状態表示を維持する。
  2. 閲覧・編集をbasiQ-uiの`Button`へ置換し、既存の完了色とbasiQ-uiのdanger tokenで公開/非公開を区別する。
- 決定: 選択肢2。閲覧はneutral outline、編集はaccent solidとし、非公開ロードマップには学習者向け閲覧ボタンを出さない。ページ名と重複する小見出し「運営」も削除する。
- 根拠: 操作の優先度と状態を追加データなしで明確にでき、独自ボタン外観を作らずbasiQ-uiの標準状態・フォーカスを利用できる。
- 影響: 新規CSSセレクタや独自コンポーネントは追加しない。2ボタンが収まるよう既存一覧グリッドの操作列幅だけを120pxから156pxへ変更する。
- 再検討する条件: 非公開状態のプレビュー機能を権限付きで実装する場合、またはbasiQ-uiに成功状態専用token/componentが追加された場合。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/AdminHomeView.ts`

## D-20260902-025 — 講習会名とシリーズ名の入力意図を例示で分ける

- 状態: superseded by D-20260902-028
- 判断が必要だった理由: push後の再確認で、「開催名」という呼称ではシリーズとの関係が伝わらず、講習会名へ回数を含める入力規則も例示から読み取れないという追加レビューがあった。
- 選択肢:
  1. 現在のラベルと例示を維持する。
  2. 任意の開催タイトルを「シリーズ名」と呼び、講習会名とシリーズ名へそれぞれ具体例を置く。
- 決定: 選択肢2。講習会名は「例: なろう講習会第六回」、シリーズ名は「例: なろう講習会」とし、シリーズの一部では第何回かも講習会名へ書くよう説明する。
- 根拠: 2つの入力の粒度を、追加画面や説明カードを作らず既存フォーム上で区別できる。指定された文言をそのまま採用できる。
- 影響: 表示文言とplaceholderだけを変更する。既存の`WorkshopInput.title`と`OccurrenceInput.title`、D1カラム、API、複製動作は変更しない。新規CSS・依存はない。
- 再検討する条件: シリーズが独立エンティティとして検索・集約・継承を必要とする具体的運用が確定した場合。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/WorkshopWizardView.ts`、`app/ui/components/OccurrenceFields.ts`

## D-20260902-026 — 回番号の単発時入力とシリーズ名の任意性をラベルへ明示する

- 状態: superseded by D-20260902-031（回番号ラベルは継続）
- 判断が必要だった理由: 追加レビューで、単発講習会の回番号と、シリーズ名を入力する条件がラベルだけでは分かりにくいと確認された。
- 選択肢:
  1. 補足説明を別段落で追加する。
  2. 入力欄のラベル自体へ短い条件を加える。
- 決定: 選択肢2。「回番号（単発講習会なら1で）」「シリーズ名（あれば）」と表示する。
- 根拠: 入力時に必要な情報を対象欄の直近で確認でき、説明過多や新しいUIを避けられる。
- 影響: ラベルだけを変更し、回番号の既定値1、シリーズ名のnullable入力、API、D1、CSSは変更しない。
- 再検討する条件: 単発/複数回を先に選ぶ入力方式へ変更し、回番号を自動決定できるようになった場合。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/components/OccurrenceFields.ts`

## D-20260902-027 — プロフィールの3切替を下線型ARIAタブとして表現する

- 状態: decided
- 判断が必要だった理由: 追加レビューで、ARIA上はタブでもbasiQ-uiの塗り/枠付きボタン外観のため、ボタン切替に見えると確認された。
- 選択肢:
  1. basiQ-uiの`Button`へ`role=tab`を付けた現状を維持する。
  2. Tabs部品がない不足分だけ、native buttonを下線型タブとして実装する。
- 決定: 選択肢2。選択タブだけaccentの下線と文字色を付け、背景塗りと囲み枠をなくす。既存の`tablist`/`tab`/`tabpanel`、roving tabindex、左右矢印/Home/End操作は維持する。
- 根拠: タブの意味・見た目・キーボード操作が一致する。basiQ-uiにTabsはなく、既存部品を誤った外観で流用するよりbasiQ tokenだけを使う最小不足部品が妥当。
- 影響: D-20260902-021のbasiQ-ui`Button`外観採用部分を置換する。再試行など通常操作は引き続きbasiQ-uiの`Button`を使う。追加依存・汎用コンポーネントはない。
- 再検討する条件: basiQ-uiに正式なTabsが追加された場合は置換する。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/ProfileView.ts`、`app/styles/pages.css`

## D-20260902-028 — 講習会を先に永続化し、保存済み画面で開催を追加する

- 状態: decided
- 判断が必要だった理由: 追加レビューで、講習会名・概要の入力後に「次へ」と進むのではなく、その時点で講習会を作成・表示し、続いて各回を個別の枠で登録する流れが求められた。既存の公開条件と講習会/開催の独立モデルを壊さず、開催0件の中間状態を扱う必要があった。
- 選択肢:
  1. 3段階ウィザードの最終操作まで講習会と開催を一括保存する。
  2. POSTだけ開催0件を許可して講習会の器を先に保存し、保存済み通常編集画面へ遷移して最初の開催を追加する。PUTでは引き続き開催1件以上を必須にする。
- 決定: 選択肢2。新規画面は講習会名・概要とbasiQ-uiの「講習会を作成」ボタンだけを表示する。作成後は`/admin/workshops/{id}?created=1`で保存済み内容、成功通知、第1回の開催カードを表示する。公開開催がない講習会は管理用GETだけで読め、学習者向け一覧・詳細からは除外する。
- 根拠: 講習会と開催のデータ境界を画面上の操作と一致させ、作成直後から連番IDを持つ実データとして扱える。空の中間状態は非公開なので、未完成情報が学習者へ漏れない。
- 影響: D-20260902-021の「最初の開催まで3段階で登録」とD-20260902-025の「講習会名へ回数を書く」部分を置換する。講習会名の例は「なろう講習会」とし、シリーズならシリーズ名を書く。開催カードは回番号を見出しに反映し、従来の複製、公開状態、教材、対象者、関係保存を維持する。新規CSS・依存・migrationはない。
- 再検討する条件: 開催を作らず放置された講習会の整理、削除、監査が内部試用で必要になった場合。
- 参照: 本人指定のtraQレビュー（17:55、17:57、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/app/api/workshops/route.ts`、`app/lib/domain.ts`、`app/ui/views/WorkshopWizardView.ts`

## D-20260902-029 — 年度を日時から導出し、10件の代表seedで班と複数回表示を確認する

- 状態: decided
- 判断が必要だった理由: 追加レビューで年度の二重入力をなくし、開催組織・班を明示すること、少数fixtureでは確認できなかったグラフィックス班・ゲーム班・Unity全3回の表示を実データで確認することが求められた。
- 選択肢:
  1. 年度入力と公開3件のseedを維持する。
  2. 日時変更時に先頭4桁を年度へ同期して年度欄を除き、公開seedを10講習会へ増やす。
- 決定: 選択肢2。日時未定時は作成時の現在年を内部値として保持し、日時が入った時点でその年へ更新する。班は「開催する組織・班」と表示する。Unity講習会は一つの講習会の下に「Unity講習会第一回」「第二回」「第三回」の3開催を持たせ、グラフィックス班を含む他分野も1回開催として追加する。
- 根拠: 同じ情報の手入力不一致を避け、中心モデルの1回/複数回を10件のカード密度で評価できる。追加seedは`INSERT OR IGNORE`なので既存ローカルデータを上書きしない。
- 影響: 新規環境の学習者向け一覧は公開10件、下書き1件になる。年度のAPI/D1列は検索・過去資産の保持に必要なため残す。日時未定の下書きも従来どおり保存できる。新規CSS・依存はない。
- 再検討する条件: 年度が暦年ではなく会計年度・活動年度を意味すると運用上確定した場合、または本番データ移行時にfixtureを分離する場合。
- 参照: 本人指定のtraQレビュー（17:48、17:50、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/components/OccurrenceFields.ts`、`app/db/setup.ts`、`app/tests/api.integration.test.mjs`

## D-20260902-030 — バッジの日付を受講完了日と呼び、詳細導線を中央に置く

- 状態: decided
- 判断が必要だった理由: push後の追加レビューで、「獲得日」では保存元の完了記録との関係が曖昧で、詳細導線の左寄せもバッジ詳細カード内で不均衡と確認された。
- 選択肢:
  1. 既存文言とfooter左寄せを維持する。
  2. 「受講完了日」へ変更し、既存の中央揃え共通レイアウトで詳細導線を中央配置する。
- 決定: 選択肢2。日付値と完了データは変更せず表示名だけを直し、footer内で既存`empty-actions`を再利用する。
- 根拠: バッジが講習会完了記録から導出される現在のモデルを正確に表現でき、専用CSSを増やさず指定配置にできる。
- 影響: `ProfileView`のバッジ詳細だけを変更する。ロードマップの同名導線、完了日時、API、D1は変更しない。新規CSS・依存はない。
- 再検討する条件: バッジ獲得条件が受講完了以外にも増え、獲得日時を別に保持する場合。
- 参照: 本人指定のtraQレビュー（18:02、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/ProfileView.ts`、`app/styles/base.css`

## D-20260902-031 — 開催のシリーズ名入力を廃止し、作成中の講習会をホームで更新確認できるようにする

- 状態: decided
- 判断が必要だった理由: 追加レビューで開催ごとのシリーズ名入力は不要とされ、講習会作成直後と保存のたびにホームで変更を確認し、未確定値は「未定」と示すことが求められた。一方、通常検索へ未公開情報を混ぜない境界も維持する必要があった。
- 選択肢:
  1. ホームと検索の両方を公開開催だけに限定し、作成中は管理画面でだけ確認する。
  2. ホーム取得だけに作成中を含め、更新日時順で表示する。通常検索と学習者向け詳細は公開開催だけに限定する。
- 決定: 選択肢2。`includeDrafts=1`をホーム専用で使用し、全開催からホーム用の班・年度・開催数を集約する。開催0件は「班 未定」「年度 未定」「開催未定」とし、未公開カードは`/admin/workshops/{id}`へ「編集を続ける」で戻す。開催フォームの任意`title`入力は削除するが、既存データと複製互換の列は削除しない。「学べること」はフォーム、詳細、検証文言を「この回で学べること」へ統一する。
- 根拠: 作成・保存結果をホーム先頭で即確認でき、未完成カードから編集を再開できる。公開検索と学習者詳細を分けるため、過去資産の発見品質や公開条件は崩れない。
- 影響: D-20260902-026のシリーズ名入力部分を置換する。Homeの件数には公開前講習会も含まれ、通常検索の件数は公開講習会だけを維持する。新規CSS・依存・migrationはない。
- 再検討する条件: 本番で運営者以外へ作成中講習会を見せない権限要件が確定した場合は、ホームの`includeDrafts`を認証済み管理者へ限定する。
- 参照: 本人指定のtraQレビュー（18:25、18:27、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/db/repository.ts`、`app/ui/views/HomeView.ts`、`app/ui/components/WorkshopSummaryCard.ts`、`app/ui/components/OccurrenceFields.ts`

## D-20260902-032 — 前後関係の複数選択を件数と明るい緑で明示する

- 状態: decided
- 判断が必要だった理由: 追加レビューで、「先に学ぶ」「次に学ぶ」を同時に1件以上選べないように見え、選択状態も背景が薄すぎて判別しづらいと確認された。実ブラウザでは両側の複数選択自体は成立していたため、モデルを広げる変更ではなく操作結果の可視化が必要だった。
- 選択肢:
  1. basiQ-ui `ToggleButton`の既定on-stateだけを維持する。
  2. 両見出しへ選択件数を表示し、選択行と実コンポーネントのon-stateを既存の成功色tokenへ対応させる。
- 決定: 選択肢2。両側は独立した配列として複数選択を維持し、重複IDを除く。同一講習会を前後へ同時指定する操作だけは直接の循環になるため、従来どおり反対側から外す。
- 根拠: 2件+1件の同時選択と保存結果をその場で確認でき、独自トグルや新しい色を作らずbasiQ-uiの状態変数と既存の完了色でレビュー意図を満たせる。
- 影響: `WorkshopWizardView`、ドメインテスト、API統合テストを更新する。新規CSSセレクタ、依存、migration、保存形式の変更はない。循環関係のAPI拒否も維持する。
- 再検討する条件: 同じ講習会を前後へ同時指定することに循環以外の運用上の意味が確定した場合、またはbasiQ-uiに成功toneのToggleButtonが追加された場合。
- 参照: 本人指定のtraQレビュー（18:32、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/WorkshopWizardView.ts`、`app/lib/domain.ts`

## D-20260902-033 — 講習会削除は影響範囲を確認してから関連データごと実行する

- 状態: decided
- 判断が必要だった理由: 追加レビューで編集画面の保存ボタン横に赤い講習会削除ボタンが求められた。過去講習会を資産として残す方針と、誤操作で開催・完了・ロードマップ関係まで失う危険を両立して扱う必要がある。
- 選択肢:
  1. 削除ボタンを表示するが、下書きだけに制限する。
  2. 明示的な管理操作として全講習会を削除可能にし、影響する関連データを確認文へ列挙して取消機会を置く。
- 決定: 選択肢2。basiQ-ui `Button`のdanger toneを使い、開催、完了記録、ロードマップ上の配置も消えることと不可逆性をnative confirmで知らせる。削除中は保存と削除を無効化し、成功後はホームへ戻す。
- 根拠: レビューで求められた管理操作を実データへ接続しつつ、通常の編集・保存と破壊操作を色、文言、確認で分離できる。D1の既存`ON DELETE`制約を使うため、部分削除や孤児レコードも残らない。
- 影響: `DELETE /api/workshops/{id}`、repository、UI API、編集画面、API統合テストを追加する。過去資産の通常表示・発見は維持されるが、管理者が確認を承諾した講習会は関連記録ごと削除される。新規CSS、依存、migrationはない。
- 再検討する条件: 認証・権限を実装する時点で、物理削除を管理者専用のアーカイブ/復元へ置き換える要件が確定した場合。
- 参照: 本人指定のtraQレビュー（18:38、18:41、18:48、18:50、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/WorkshopWizardView.ts`、`app/db/repository.ts`

## D-20260902-034 — 開催削除は共通保存の差分として扱い、最後の1件を保護する

- 状態: decided
- 判断が必要だった理由: 追加レビューで開催カードごとの「−」削除が求められた。専用削除APIを増やすか、講習会/開催を同時編集する既存PUTの意味を完成させるか、また開催0件を通常更新で許すかを決める必要があった。
- 選択肢:
  1. 開催専用DELETE APIを追加し、各ボタンから即時削除する。
  2. フォームから除外した開催を講習会PUT時の差分として削除し、最後の1件だけはUIとドメイン制約の両方で保護する。
- 決定: 選択肢2。各カードにbasiQ-ui danger outlineの「−」を置き、2件以上のときだけ操作可能にする。repositoryは入力に残らない既存開催を同じD1 batchで削除し、他開催・関係の保存と原子的に反映する。
- 根拠: ウィザードと通常フォームが同じ講習会/開催データへ保存される設計を維持でき、削除だけ先に確定して保存失敗時に部分反映されることを避けられる。単発は第1回だけというレビュー文言とも一致する。
- 影響: `OccurrenceFields`、`WorkshopWizardView`、repository、API統合テストを更新する。専用API・依存・migrationはない。ホームカードの高さずれにはbasiQ-ui Cardを行高へ伸ばす共通レイアウト規則1件だけを追加し、独自外観は作らない。
- 再検討する条件: 開催単位の監査・復元・権限や、開催0件で講習会を維持する運用が確定した場合。
- 参照: 本人指定のtraQレビュー（19:06、19:08、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/components/OccurrenceFields.ts`、`app/ui/components/WorkshopSummaryCard.ts`、`app/db/repository.ts`

## D-20260902-035 — 編集の初期読込失敗時はフォームを隠して再試行へ限定する

- 状態: decided
- 判断が必要だった理由: 追加レビューで、サーバーへ接続できないときに未取得の編集情報を表示せず、再試行だけを提示することが求められた。保存中の失敗とは異なり、初期読込失敗では正しい編集対象を構成できない。
- 選択肢:
  1. 空フォームと失敗feedbackを同時に表示し、利用者が入力を始められるようにする。
  2. 初期読込失敗を専用状態にし、フォーム全体を隠して再試行だけを出す。
- 決定: 選択肢2。`loadFailed`を読込中・保存失敗と分け、同じ`load`を呼ぶbasiQ-ui neutral outlineの再試行ボタンを表示する。開催追加は記号だけのoutlineからaccent solidの「開催データを追加」へ置換する。
- 根拠: 未取得情報を正しい値に見せて上書きする事故を防ぎ、利用者の次の操作を明確にできる。basiQ-uiの既存feedback/Button状態で実現できる。
- 影響: `WorkshopWizardView`と、モバイルで追加ボタンの文言を保つレイアウト規則1件を更新する。保存失敗時は入力保持を維持し、初期読込失敗時だけフォームを隠す。新規依存・migrationはない。
- 再検討する条件: オフライン編集やローカルキャッシュからの復元を、データ版管理付きで実装する場合。
- 参照: 本人指定のtraQレビュー（19:13、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/WorkshopWizardView.ts`

---

## 追記テンプレート

## D-YYYYMMDD-NNN — 判断の題名

- 状態: decided | pending | superseded
- 判断が必要だった理由:
- 選択肢:
  1.
  2.
- 決定:
- 根拠:
- 影響:
- 再検討する条件:
- 参照:
