# Decision Log

このファイルは、α版の実装中に必要になった重要な意思決定と、その理由を後から追跡できるように残す。

## 記録ルール

- ローカルで可逆な事項は、合理的に決定して`decided`として記録し、作業を続ける。
- 確認必須事項がブロッカーになった場合だけ`pending`を使用する。
- 決定を変更した場合は過去の記録を削除せず、旧記録を`superseded`にして新しい決定へリンクする。
- 単純な実装詳細をすべて記録するのではなく、別の担当者が「なぜこの形か」を知る必要がある判断を記録する。

## D-20260826-001 — 承認不要の範囲では自律的に完成まで進める

- 状態: decided
- 判断が必要だった理由: 長時間実行中に細かな確認待ちで停止せず、α版の完成まで継続できる運用が必要だった。
- 選択肢:
  1. 未決事項ごとに作業を止めて本人へ確認する。
  2. 承認境界を定義し、その内側では合理的な仮定を置いて自律的に進める。
- 決定: 選択肢2。ローカル、可逆、無料、目的とスコープの範囲内にある判断は自律的に行う。
- 根拠: α版では細部の最適化より、価値循環を端から端まで成立させて検証可能にすることを優先するため。
- 影響: 技術構成、データモデルの詳細、UIの細部、実装順序、必要な一般的OSS依存、旧デモの移植範囲について、原則として質問せず決定できる。
- 再検討する条件: 外部公開、費用、外部書き込み、個人データ、認証情報、破壊的操作、重大なスコープ変更が必要になった場合。
- 参照: `AGENTS.md`、`PROJECT_BRIEF.md`、`PLAN.md`

## D-20260826-002 — traP内の情報源はブラウザで読み取り専用利用する

- 状態: decided
- 判断が必要だった理由: 講習会の雰囲気、用語、既存運用を理解するため、内部情報源を調査に使う範囲を明確にする必要があった。
- 選択肢:
  1. ローカル資料だけで推測する。
  2. `q.trap.jp`と`wiki.trap.jp`を読み取り専用で閲覧・検索する。
- 決定: 選択肢2。ブラウザによる閲覧とサイト内検索に限定して利用する。
- 根拠: 文脈を確認しつつ、外部状態や第三者の情報へ不要な変更を加えないため。
- 影響: 投稿、編集、コメント、リアクション、投票、ファイル追加、設定変更などは行わない。実装判断に使った情報は必要最小限の要約とURLを本ログへ残す。
- 再検討する条件: 閲覧以外の操作が必要になった場合、または情報の取り扱いに不明点が生じた場合。
- 参照: `AGENTS.md`

## D-20260826-003 — α版の主要導線を6ステップに固定する

- 状態: decided
- 判断が必要だった理由: 広い構想の各領域を深掘りせず、最初に端から端まで検証できる一本道を定義する必要があった。
- 選択肢:
  1. 運営・学習者・推薦・共同編集をそれぞれ独立機能として広く作る。
  2. 登録から学習記録までを6ステップに限定してつなぐ。
- 決定: 選択肢2。①運営が基本情報を入力、②前提・後続を選んで登録、③学習者が検索・カテゴリで発見、④詳細から関係をたどる、⑤受講完了を記録、⑥プロフィールとバッジを確認、の6ステップとする。
- 根拠: `PROJECT_BRIEF.md`の最初のマイルストーンと、Vault記録の中心価値が一致しているため。
- 影響: 教材配信、ランキング、自動推薦、共同編集、汎用DSLは導線から外す。内部試用ではこの6ステップを主なフィードバック単位にする。
- 再検討する条件: 内部試用で、6ステップの途中に不可欠な作業が欠けていると判明した場合。
- 参照: `PROJECT_BRIEF.md`、`PLAN.md`、Vault `Handoff.md` / `Project.md`

## D-20260826-004 — Sites + D1を基盤とし、basiQ-uiのVue UIを統合する

- 状態: decided
- 判断が必要だった理由: ローカルで再現でき、構造化データを永続化できる基盤と、利用するUIライブラリを確定する必要があった。
- 選択肢:
  1. 旧デモのGo/Vue構成をそのまま拡張する。
  2. SitesのVinext/D1基盤へVueのUI islandを載せ、`basiq-ui`を利用する。
  3. Reactだけで作り、`basiq-ui`の見た目だけを模倣する。
- 決定: 選択肢2。API・D1接続・配信はSites/Vinext、利用者向けUIはVue 3と`basiq-ui@0.1.0-beta.0`で実装する。
- 根拠: D1によりローカルと将来のSites環境で同じ永続化モデルを使え、本人指定の`basiq-ui`はVue 3をpeer dependencyとするため。`basiq-ui`はMIT、依存する`reka-ui`もMITであることを確認した。
- 影響: `.openai/hosting.json`はD1 binding `DB`を持つ。`basiq-ui`は公開直後のパッケージを制限する環境で通常解決できなかったため、参照デモで使用・integrity確認済みの同一成果物を`app/vendor`へライセンス込みで固定し、lockfileから再現できるようにする。外部公開は行わない。
- 再検討する条件: Vue islandとVinextの組み合わせでビルドまたはアクセシビリティ上の解消不能な問題が発生した場合、または`basiq-ui`の安定版・React対応版が提供された場合。
- 参照: `app/package.json`、`app/package-lock.json`、`app/.openai/hosting.json`、参照デモ `web/package.json`

## D-20260826-005 — 認証なしの固定試用ユーザーと6テーブルで始める

- 状態: superseded（固定試用ユーザーは継続、6テーブル構成はD-20260902-011/D-20260902-013で置換）
- 判断が必要だった理由: 個人データや外部認証を扱わずに、講習会・関係・受講履歴・バッジの整合性を検証できる最小モデルが必要だった。
- 選択肢:
  1. 本人認証と複数ユーザー管理まで実装する。
  2. 非個人情報の固定試用ユーザーをseedし、認証はα版の外とする。
- 決定: 選択肢2。`users`、`workshops`、`workshop_relations`、`completions`、`badges`、`user_badges`の6テーブルをD1へ永続化する。関係は「前提→後続」の有向辺とし、自己参照・重複・循環を拒否する。
- 根拠: 認証方式と個人データの取り扱いは明示承認が必要だが、固定personaなら主要な価値循環を安全に検証できるため。
- 影響: UIには内部試用の固定personaであり認証ではないことを示す。IDは安定した文字列、日時はUTC ISO 8601文字列を使う。登録済み講習会は即時に一覧へ出す。
- 再検討する条件: α試用後に認証方式と個人データ方針が承認された場合。
- 参照: `PROJECT_BRIEF.md`の制約、`app/db/schema.ts`

## D-20260826-006 — 検索・バッジ・旧デモ移植を最小仕様に限定する

- 状態: superseded（D-20260902-013/D-20260902-015で置換）
- 判断が必要だった理由: 探索、達成感、旧デモ再利用を、α版の価値検証に必要な深さへ絞る必要があった。
- 選択肢:
  1. FTS、複数バッジ、Markdown DSL Parserを初回から組み込む。
  2. 部分一致とカテゴリ、初回完了バッジ1個、固定フォームで始める。
- 決定: 選択肢2。検索はタイトル・概要・説明の部分一致とカテゴリ完全一致、バッジは完了1件以上で得る「はじめの一歩」1個とする。完了取消で0件になればバッジも取り消す。旧デモからはRenderer分離、入力保持、エラー表示、テスト観点を採り入れ、Parser/Run/CompletionViewのコードは移植しない。
- 根拠: 登録フォームの項目が固定された現段階ではDSLの複雑さが価値循環の検証に寄与せず、検索とバッジも最小規則の方が一貫性を自動テストしやすいため。
- 影響: 完了登録は冪等、プロフィールの履歴とバッジは常に整合させる。旧デモ本体は読み取り専用のまま保持する。
- 再検討する条件: 内部試用で、入力定義の頻繁な変更、検索精度不足、段階的達成の必要性が確認された場合。
- 参照: 参照デモ監査、`PROJECT_BRIEF.md`の「今はやらないこと」

## D-20260826-007 — 自動テストとブラウザ一巡をリリースゲートにする

- 状態: decided
- 判断が必要だった理由: 各画面が個別に表示できるだけでなく、永続化を含む一本道が壊れていないことを再現可能に保証する必要があった。
- 選択肢:
  1. 手動確認だけで停止条件を判定する。
  2. ドメイン/APIテスト、型検査、lint、buildに加え、主要導線のブラウザE2Eを必須にする。
- 決定: 選択肢2。空DBへの初期化、再読込、検索、関係制約、完了の冪等性、バッジ整合性、入力エラー、主要6ステップを自動・ブラウザ双方で検証する。
- 根拠: D1、API、Vue UIの境界をまたぐ不具合は単一層のテストだけでは検出できないため。
- 影響: 中核シナリオのskipやリトライ依存を残さない。全ゲートが成功してからチェックポイント6を完了とする。
- 再検討する条件: 実行環境の制約でブラウザ自動化が不可能な場合。ただし代替の実ブラウザ操作記録を必須とする。
- 参照: `PLAN.md`停止条件

## D-20260826-008 — migrationを起動時スキーマの正本にし、統合テストを隔離する

- 状態: decided
- 判断が必要だった理由: 手書きDDLとDrizzle migrationのずれ、および共有D1を使うテストによる試用データ破壊を防ぎ、新規環境と再起動後の永続化を再現可能に検証する必要があった。
- 選択肢:
  1. 起動用DDL、Drizzle schema、migrationを別々に保守し、既存の開発DBでAPIテストする。
  2. checked-in migrationを起動時にも適用し、適用台帳を持たせ、テストごとに一時D1を起動する。
- 決定: 選択肢2。`drizzle/0000_red_sleepwalker.sql`を`db/migrations.ts`から読み込み、`alpha_schema_migrations`で適用を管理する。API統合テストは一時ディレクトリのD1でサーバーを起動・再起動し、終了時にその一時領域だけを破棄する。
- 根拠: migrationと実際の初期化経路を一致させ、普段の`.wrangler`状態に触れずに空DB、再読込、関係のrollback、完了・バッジ境界を検証できるため。
- 影響: `npm run test:api`単体で主要API導線を再現できる。関係は前提・後続合計20件、ID 120文字に制限し、循環判定は非再帰化した。並行完了時の新規バッジ応答は`INSERT ... RETURNING`で実際に挿入した行だけを返す。
- 再検討する条件: migrationが複数になったときは、新しいSQLを適用リストへ追加し、既存DBからの段階移行を統合テストへ加える。
- 参照: `app/db/migrations.ts`、`app/db/setup.ts`、`app/tests/api.integration.test.mjs`

## D-20260826-009 — 公開経過期間を守りつつ依存脆弱性を解消する

- 状態: decided
- 判断が必要だった理由: scaffold時点のNext/Vinext/Vite/Cloudflare依存にhighの既知脆弱性があり、一方で公開直後3日未満の最新版は採用しない制約があった。
- 選択肢:
  1. scaffoldの固定版を維持し、既知問題をそのまま受け入れる。
  2. 互換範囲内かつ公開後3日以上の修正版へ更新し、Nextの推移依存だけ安全版へoverrideする。
  3. 公開当日の最新版へ全面更新する。
- 決定: 選択肢2。Nextは公開経過期間を満たす`16.2.12`、React系は`19.2.8`、Vinextは`1.0.0-beta.8`、Viteは`8.2.2`、Cloudflare pluginは`1.53.1`へ揃え、PostCSSとSharpを監査済み版へoverrideした。
- 根拠: peer dependencyを満たしたままbuild・統合テストが成功し、`npm audit --omit=dev`を0件にできたため。
- 影響: 開発専用`drizzle-kit@0.31.10`には古いesbuild-loader由来のmoderate 4件が残る。監査が示す唯一の自動修正は非互換な`0.18.1`へのdowngradeなので採用せず、migration生成は信頼できるローカルschemaだけに限定する。
- 再検討する条件: 現行Drizzle schemaと互換な修正版が公開後3日を経過した場合。
- 参照: `app/package.json`、`app/package-lock.json`、`STATUS.md`

## D-20260826-010 — α版の操作文脈はURLとタブ内下書きで保持する

- 状態: decided
- 判断が必要だった理由: 検索から詳細へ移ると条件が消え、登録中の再読み込みで長文入力が失われると、主要導線の試用が中断されるため。
- 選択肢:
  1. 画面コンポーネントのメモリだけで状態を保持する。
  2. 検索条件はURL、登録下書きは`sessionStorage`へ保存し、個人情報を扱う永続アカウント保存は行わない。
- 決定: 選択肢2。検索・カテゴリをquery parameterへ同期し、詳細の戻る導線へ引き継ぐ。登録下書きは同じタブのsession中だけ復元し、登録成功または明示破棄で消す。
- 根拠: 外部サービスや個人データを増やさず、誤操作・再読み込みから回復できるため。
- 影響: SPA遷移時の`document.title`、mainフォーカス、`aria-current`、フィールドエラー関連付け、モバイル主要ナビも同時に整備した。OGカードはローカル用メタデータへ設定したが、外部公開はしていない。
- 再検討する条件: 複数端末・複数利用者の認証と下書き同期方針が承認された場合。
- 参照: `app/ui/AlphaApp.ts`、`app/ui/views/WorkshopListView.ts`、`app/ui/views/WorkshopWizardView.ts`、`app/public/og.png`

## D-20260902-011 — 講習会と開催を分離し、講習会を連番IDで識別する

- 状態: decided
- 判断が必要だった理由: α版の1テーブル1講習会では、複数回、再放送、年度ごとの独立教材を一つの資産として扱えない。
- 選択肢:
  1. α版の文字列ID付き講習会を開催として流用する。
  2. 連番INTEGER（SQLite上の64-bit signed integer）を持つ講習会と、その下の連番開催へ分離する。
- 決定: 選択肢2。URLは`/workshops/{id}`、人が理解する回は`#1`、`#2`で表し、開催の内部IDはURLフラグメントに露出しない。
- 根拠: 記入済み担当Aの決定と今回の明示指示に一致し、1回開催では階層を隠しながら複数回を同じページへまとめられる。
- 影響: αテーブルと利用者のローカルデータは破壊せず、追加migrationでβテーブルを作る。検索、完了、ロードマップは講習会、日時・教材・対象・班・年度・公開状態は開催を参照する。
- 再検討する条件: 本番DBがSQLite/D1以外になり、BIGINTの表現方法を変える必要が生じた場合。
- 参照: 記入済み詳細設計A1、今回依頼

## D-20260902-012 — 開催の複製は値コピー後に完全独立させる

- 状態: decided（Flowの複製方法はD-20260905-086で更新）
- 判断が必要だった理由: 再放送・次回を素早く作りつつ、後の変更を元開催へ波及させないルールが必要だった。
- 選択肢:
  1. 共通情報を参照し続け自動同期する。
  2. 利用者入力項目を全コピーし、種別とコピー元だけを関係として保持する。
- 決定: 選択肢2。複製後は通常編集ですべて変更でき、同期しない。完了・参加者・履歴などのシステム記録はコピーしない。
- 根拠: 担当Aの確定方向であり、過去資産を安全な出発点として再利用できるため。
- 影響: コピー元削除は実装しない。コピー元参照は任意外部キーとして残し、通常回番号は非再放送の開催だけで採番する。
- 再検討する条件: 一括更新の具体的な利用例と必要性が内部試用で確認された場合。
- 参照: 記入済み詳細設計A1、今回依頼

## D-20260902-013 — βの公開・完了・バッジ・ロードマップ状態を重複保存しない

- 状態: decided（完了をLecture単位にした部分はD-20260904-060でsuperseded。導出状態を重複保存しない原則は継続）
- 判断が必要だった理由: 記入済み資料に未決・矛盾があり、中心体験を一貫させる最小規則が必要だった。
- 選択肢:
  1. 開催状況、バッジ、ロードマップ進捗を別々に保存する。
  2. 開催は`draft/published`だけを保存し、予定/過去は日時、バッジとロードマップ進捗は講習会完了から導出する。
- 決定: 選択肢2。講習会は公開開催を1件以上含むとき発見可能。完了は本人による講習会単位のトグルで、開催方法は区別しない。完了した公開講習会ごとにバッジを表示する。
- 根拠: 詳細設計A2/E1〜E4の多数意見と「過去の講習会を資産にする」を最少の状態で満たす。
- 影響: 下書きへ戻しても完了事実は保持するが、学習者向け一覧・詳細・プロフィール表示からは公開開催がない講習会を隠す。削除はβスコープ外。
- 再検討する条件: 権限、監査履歴、総集編による部分完了が具体仕様として確定した場合。
- 参照: 記入済み詳細設計A2、E1〜E4

## D-20260902-014 — βは既存依存だけで実装し、basiQ-uiの実部品を操作の正本にする

- 状態: decided
- 判断が必要だった理由: UI品質を上げつつ、依存追加と独自デザインシステム化を避ける必要があった。
- 選択肢:
  1. 新しいUI/フォームライブラリを追加する。
  2. 導入済み`basiq-ui`のThemeProvider、Button、Card、ChoiceGroup、ChoiceGroupItem、ToggleButtonを優先し、不足する入力レイアウトだけ共通CSSにする。
- 決定: 選択肢2。新規依存は追加しない。
- 根拠: パッケージのREADME、型定義、実装済み利用箇所を監査し、βに必要な選択・操作部品を満たせると判断した。
- 影響: PCと390px、キーボード、フォーカス、通常/読込/0件/失敗/保存中/保存失敗をリリースゲートにする。
- 再検討する条件: basiQ-uiにない複雑な部品が中心体験で必要になった場合。
- 参照: `app/node_modules/basiq-ui/README.md`、`app/node_modules/basiq-ui/dist/index.d.ts`

## D-20260902-015 — αの挙動知見だけをkeepし、UI・データ境界をβとして再設計する

- 状態: decided
- 判断が必要だった理由: α版の画面、情報設計、文言、巨大CSS、コンポーネント構造を継承すると、記入済み設計と内部ツールとしての品質を満たせないという評価が確定した。
- 選択肢:
  1. αの画面へβ機能を継ぎ足す。
  2. keep / rewrite / removeを明示し、動作知見を使ってβのUI・型・データ境界を再設計する。
- 決定: 選択肢2。
  - keep: Vinext/D1のローカル基盤、migration適用、固定試用persona、入力保持・URL状態・フォーカス管理の観点、隔離D1/API統合テストの観点。
  - rewrite: 講習会/開催/ロードマップの型とAPI境界、repository、Vueのアプリシェル・全主要ビュー・共有開催フォーム、情報設計、文言、CSS。
  - remove: `AlphaApp`、1,034行の単一`globals.css`、カテゴリ中心の探索、件数バッジを目的化した表示、αの文字列講習会IDと1行1講習会をβ画面へ流す経路。αテーブル自体は利用者データ保護のため削除しない。
- 根拠: 記入済み設計と今回の品質条件では、過去教材を探して使う行動、単発時の階層非表示、複数回/再放送の同一ページ閲覧が中心であり、αの表示構造とは異なるため。
- 影響: CSSはbasiQ-ui本体と`base/layout/forms/pages`へ分け、ThemeProvider、Button、Card、ChoiceGroup/Item、ToggleButtonを実部品として使用する。カード、角丸、装飾統計は情報の境界または操作上必要な箇所だけに限定する。旧D-20260826-006の検索・バッジ・固定フォーム仕様は置換する。
- 再検討する条件: 内部試用で、現在の情報密度や画面境界が実際の運用に合わない具体例が得られた場合。
- 参照: 今回の重要方針修正、`app/ui/BetaApp.ts`、`app/styles/`

## D-20260902-016 — 内部サイトは今回の実装判断に使用しない

- 状態: superseded（本人が指定したtraQチャンネルの読み取り専用レビューをD-20260902-020で許可・採用）
- 判断が必要だった理由: `q.trap.jp`と`wiki.trap.jp`は読み取り専用で参照可能だったが、記入済み詳細設計とローカル資料だけでβ中心体験を確定できた。
- 選択肢:
  1. 認証済み内部サイトを追加調査する。
  2. 必要性のない非公開情報へのアクセスを避ける。
- 決定: 選択肢2。両サイトは閲覧せず、コード・文書へ非公開情報を取り込んでいない。
- 根拠: 今回の設計判断に不足がなく、必要最小限の情報利用に留めるため。
- 影響: 内部サイト由来のURL・内容・引用は成果物に含まれない。
- 再検討する条件: 実運用用語についてローカル資料だけでは判断できない具体的な疑問が生じた場合。
- 参照: `AGENTS.md`、今回依頼

## D-20260902-017 — 生成画像は情報設計の方向として使い、架空機能は実装しない

- 状態: superseded（条件検索の比較行だけD-20260902-018でカード表示へ変更。その他の判断は継続）
- 判断が必要だった理由: `BETA_DESIGN_QUESTIONS.md`等に貼られた生成画像は現行βより明確な画面階層を示す一方、未実装のカテゴリ、作者、共有、推薦、編集機能やサンプル値も含み、そのまま仕様にすると未接続モックが増える。
- 選択肢:
  1. 生成画像を画面仕様として忠実に複製し、不足データは仮値で補う。
  2. 画像群に共通する情報密度、ナビゲーション、列構成、状態色だけを採用し、文言・項目・操作は実APIと記入済み設計へ合わせる。
- 決定: 選択肢2。PCは約240pxの薄灰左ナビと白い作業面、発見はカード壁でなく比較行、詳細/ロードマップ/学びの記録は主列＋右レールとする。390pxでは左ナビを上部ブランド＋固定下部ナビへ変換し、絞り込みを`details`で畳み、右レールを主内容より先に縦積みする。
- 根拠: 9枚の画像、Questions、Worksheet、生成プロンプトを照合すると、この骨格は一貫している。一方で各画像は議論用であり、現在のD1/APIにない表示を足す根拠にはならない。
- 影響: `/`を実データのホームとして分離し、`/workshops?view=roadmaps`を実切替可能な発見面にした。basiQ-uiの公開6部品は用途に合う箇所で継続し、ライブラリに存在しないナビ、タブ、入力、一覧、進捗はsemantic HTMLとbasiQトークンで実装した。新規依存は追加していない。成功色だけはbasiQにトークンがないため、完了状態専用の最小共通変数を追加した。
- 再検討する条件: 内部試用で情報密度、モバイル下部ナビ、詳細レールの順序に具体的な支障が確認された場合、またはbasiQ-uiに対応部品・成功色tokenが追加された場合。
- 参照: Vault `BETA_DESIGN_QUESTIONS.md`、`BETA_DESIGN_WORKSHEET.md`、`IMAGE_GENERATION_PROMPTS.md`、`assets/beta-design/*.png`、`app/ui/`、`app/styles/`

## D-20260902-018 — 条件検索結果は密度を保ったBasiqCardで表示する

- 状態: decided
- 判断が必要だった理由: 比較行として実装した講習会検索結果について、本人からカード表示を試す明示依頼があった。
- 選択肢:
  1. D-20260902-017の比較行を維持する。
  2. 実データの比較項目を残しつつ、実際の`BasiqCard`で各講習会を一つのリンクカードにする。
- 決定: 選択肢2。タイトル、概要、班、年度、開催数だけを載せ、カード全体を詳細リンクにする。PCは3列、1050px以下は2列、760px以下は1列にする。カード本体の背景、境界、hoverは上書きせず、`BasiqCard`の標準props、slot、surface表現をそのまま使う。
- 根拠: 今回の明示依頼を小さく可逆なUI実験として反映でき、架空項目や装飾を増やさずカードの見つけやすさを評価できるため。
- 影響: D-20260902-017の「条件検索を比較行にする」部分だけを置換する。ロードマップ一覧、ホームの新着、詳細画面の情報構造は変更しない。フォーカスはカードを包む単一リンクに置き、読み上げ順は見出し、概要、班、年度、開催数、詳細の順とする。
- 再検討する条件: 講習会数が増えた内部試用で一覧性や走査速度が比較行より悪いと確認された場合。
- 参照: 今回依頼、`app/ui/views/WorkshopListView.ts`、`app/styles/pages.css`

## D-20260902-019 — 発見入口を分け、ホームと検索で班マーク付き講習会カードを共有する

- 状態: decided
- 判断が必要だった理由: ホームに検索方法と登録方法を大きく並べた結果、学習者の主目的である講習会の発見より運営導線が強く、同じ講習会もホームでは行、検索ではカードという不一致があった。また「学びの記録」より利用者ページ全体を表す名称が必要だった。
- 選択肢:
  1. ホームの大きな条件検索/ロードマップ検索/登録パネルと、画面内の探し方タブを維持する。
  2. 条件検索とロードマップを共通ナビから直接開く独立入口にし、ホームは最近の講習会と検索導線へ絞り、登録は共通ナビの運営操作だけにする。
- 決定: 選択肢2。「学びの記録」は「プロフィール」へ改称する。ホームと条件検索は同じ共有`WorkshopSummaryCard`を使い、実際の`BasiqCard`のheader/body/footerへタイトル、概要、班、年度、開催数を配置する。名称左は汎用教材アイコンでなく、最初の担当班を`Web`、`Sys`、`アル`のように短縮した班マークにする。カード本体のsurface、境界、hoverは独自CSSで変更しない。
- 根拠: 共通ナビだけで検索、ロードマップ、プロフィール、登録へ到達でき、ホームでは実データの講習会が主役になる。班の正式名称を本文にも残すため、短縮マークは装飾的な識別補助に限定できる。
- 影響: D-20260902-017のホームに2種類の発見入口を置く判断と、D-20260902-018のホーム新着を変更しない判断をこの範囲で置換する。`/workshops?view=roadmaps`とロードマップ機能自体は維持するが、「ロードマップから探す」という画面内切替は削除する。新規依存はない。
- 再検討する条件: 担当班が複数ある講習会で単一マークが誤解を生む、または公式の班アイコンセットがbasiQ-ui/traPデザイン資産として提供された場合。
- 参照: 今回依頼、`app/ui/components/WorkshopSummaryCard.ts`、`app/ui/views/HomeView.ts`、`app/ui/views/WorkshopListView.ts`、`app/ui/BetaApp.ts`

## D-20260902-020 — ホームを検索の起点にし、作成・管理操作を運営向けページへ集約する

- 状態: decided
- 判断が必要だった理由: 指定された内部レビューでは、ホームから全講習会を同じページで見たい、検索バーをホームへ置きたい、検索専用ナビを減らしたい、学習者向けロードマップ閲覧と運営の管理操作を分けたい、という利用行動が一貫して示された。
- 選択肢:
  1. ホーム4件＋「すべて見る」、検索専用ナビ、個別の作成導線、閲覧専用ロードマップを維持する。
  2. ホームへ検索バーと公開全件を置き、運営向けページへ講習会作成とロードマップ管理を集約し、ロードマップCRUDをD1/APIへ接続する。
- 決定: 選択肢2。学習者ナビはホーム、ロードマップ、プロフィールに絞る。ホーム検索は既存`/workshops?q=`へ接続する。`/admin`を運営の入口とし、ロードマップは閲覧URLと管理URLを分け、段階・講習会順・公開状態を作成/編集/削除できるようにする。
- 根拠: ホームで検索と一覧走査が完結し、運営操作は役割別にまとまる。ロードマップは既存のD1テーブルをそのまま正本にでき、未接続モックを増やさず実装できる。
- 影響: D-20260902-019の「共通ナビから条件検索を直接開く」と「ホームは最近の講習会」という部分を置換する。ロードマップ削除は関連する段階/項目をcascade削除するため編集画面で明示確認する。認証未実装のローカルβであることは継続し、本番権限設計は別途必要。
- 再検討する条件: 公開承認、共同編集、段階/講習会のドラッグ並べ替え、監査履歴が内部試用で必要と確認された場合。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/HomeView.ts`、`app/ui/views/AdminHomeView.ts`、`app/ui/views/RoadmapEditorView.ts`、`app/app/api/roadmaps/`

## D-20260902-021 — 新規作成は案内付きへ統一し、プロフィールを3タブとして完成させる

- 状態: decided（プロフィール判断は継続。講習会作成方法はD-20260905-086でsuperseded）
- 判断が必要だった理由: 追加の内部レビューで、新規作成時に「案内付き」と「通常フォーム」を選ばせる必要性が薄く、プロフィールの3領域もタブとして明確に扱うことが求められた。
- 選択肢:
  1. 新規作成の二経路と、現状のプロフィール表示を維持する。
  2. 新規作成を案内付きウィザードへ統一し、既存講習会の全項目編集だけを一画面の通常編集として残す。プロフィールは3タブのARIA/キーボード操作を完成させる。
- 決定: 選択肢2。`/admin/workshops/new?mode=form`への導線と入力方法タブを削除し、旧URLへ直接アクセスしても同じ案内付き画面を表示する。プロフィールは「バッジ」「完了した講習会」「ロードマップ」の3タブを維持し、重複説明を削除する。
- 根拠: 初回登録は情報単位を順番に確認できる一経路へ絞るほうが迷いがなく、複製後を含む既存データの全項目変更は通常編集画面で引き続き満たせる。タブは既存のbasiQ-uiボタンを使い、独自の外観CSSを追加せず操作意味だけを強化できる。
- 影響: D-20260902-020の「案内付き/通常フォームを運営向けページへ集約する」部分を置換する。保存API、`WorkshopInput`、下書き復元、編集・複製、D1モデルは変更しない。
- 再検討する条件: 内部試用で、熟練運営者の新規一括入力が明確な時間短縮になる具体例が得られた場合。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/ProfileView.ts`、`app/ui/views/AdminHomeView.ts`、`app/ui/views/WorkshopWizardView.ts`

## D-20260902-022 — 教材導線を本文へ一本化し、通常開催を無標示の既定状態にする

- 状態: superseded（複数開催の教材配置はD-20260903-040、教材のデータ型はD-20260905-089で置換。通常開催を無標示にする判断は維持）
- 判断が必要だった理由: 追加の内部レビューで、本文と学習状況カードに同じ教材リンクが重複し、通常開催にも毎回種別ラベルが付くことで詳細画面の情報量が増えていると確認された。
- 選択肢:
  1. 重要操作として教材リンクを2か所に置き、全開催へ種別ラベルを表示する。
  2. 教材リンクは本文の「教材」節へ一本化し、通常開催はラベルなし、再放送・総集編だけを例外ラベルで示す。
- 決定: 選択肢2。同じ回の通常開催と再放送は、再放送側だけのラベルと教材補助文で区別する。保存済みの`kind`と`copiedFromOccurrenceId`は変更しない。
- 根拠: 教材の正本となる表示場所が一つになり、通常状態の反復表示を減らせる一方、担当Aで確定した「通常開催と再放送を回の中で区別する」要件は例外側の明示で満たせる。
- 影響: 学習状況カードは完了状態と完了操作だけを扱う。教材未登録状態は本文の「教材」節だけで表示する。新規CSSと依存は追加せず、通常ラベル用の未使用スタイルを削除する。
- 再検討する条件: 教材への主要導線が見つからないという実利用の観察、または通常以外の開催種別が増えて凡例が必要になった場合。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/WorkshopDetailView.ts`

## D-20260902-023 — ロードマップの段階文言を廃止し、公開状態をbooleanへ移行する

- 状態: decided
- 判断が必要だった理由: 追加の内部レビューで、段階名・説明は学習者が講習会を選ぶうえで冗長であり、公開状態も文字列二値ではなくbooleanとして保持する方針が示された。既存試用D1を破壊せず移行する方法も必要だった。
- 選択肢:
  1. 段階名・説明と`draft/published`文字列を維持し、表示だけ減らす。
  2. 段階名・説明を入力型・API・表示から外し、講習会を直接並べる。公開状態の正本を`published INTEGER`へ移し、旧列は互換用途だけに残す。
- 決定: 選択肢2。段階は講習会の順序を保持する内部のまとまりとして残すが、名称・説明を意味データとして扱わない。新規保存では旧`title/description`へ空値を書き、公開判定は常にboolean列を読む。旧`status`列は既存テーブルの`NOT NULL`制約を壊さないため同値を影書きするが、アプリケーションの読取元にはしない。
- 根拠: 講習会名へ視線を直接誘導でき、APIとUIの不要フィールドを同時に除去できる。追加カラムとbackfillだけのmigrationなら、既存のロードマップ、講習会、完了記録を削除せず移行できる。
- 影響: `RoadmapInput.published`は厳密なbooleanとして検証する。学習者画面は全段階の項目を順番どおり平坦化し、basiQ-uiの`ToggleButton`が管理画面の公開操作になる。新規依存・独自UI部品・新規CSSセレクタは追加しない。
- 再検討する条件: 段階ごとの到達目標や公開範囲が、実運用上の必須情報として具体例とともに確認された場合、または本番移行時に互換列を削除できるDB保守時間が確保された場合。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/drizzle/0002_roadmap_published_boolean.sql`、`app/ui/views/RoadmapDetailView.ts`、`app/ui/views/RoadmapEditorView.ts`

## D-20260902-024 — ロードマップ管理一覧の操作と公開状態を部品・色で区別する

- 状態: decided
- 判断が必要だった理由: push後の再確認で、一覧の文字リンクでは閲覧・編集が操作に見えにくく、公開状態と非公開状態の識別も弱いという追加レビューがあった。
- 選択肢:
  1. 文字リンクと無彩色の状態表示を維持する。
  2. 閲覧・編集をbasiQ-uiの`Button`へ置換し、既存の完了色とbasiQ-uiのdanger tokenで公開/非公開を区別する。
- 決定: 選択肢2。閲覧はneutral outline、編集はaccent solidとし、非公開ロードマップには学習者向け閲覧ボタンを出さない。ページ名と重複する小見出し「運営」も削除する。
- 根拠: 操作の優先度と状態を追加データなしで明確にでき、独自ボタン外観を作らずbasiQ-uiの標準状態・フォーカスを利用できる。
- 影響: 新規CSSセレクタや独自コンポーネントは追加しない。2ボタンが収まるよう既存一覧グリッドの操作列幅だけを120pxから156pxへ変更する。
- 再検討する条件: 非公開状態のプレビュー機能を権限付きで実装する場合、またはbasiQ-uiに成功状態専用token/componentが追加された場合。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/AdminHomeView.ts`

## D-20260902-025 — 講習会名とシリーズ名の入力意図を例示で分ける

- 状態: superseded by D-20260902-028
- 判断が必要だった理由: push後の再確認で、「開催名」という呼称ではシリーズとの関係が伝わらず、講習会名へ回数を含める入力規則も例示から読み取れないという追加レビューがあった。
- 選択肢:
  1. 現在のラベルと例示を維持する。
  2. 任意の開催タイトルを「シリーズ名」と呼び、講習会名とシリーズ名へそれぞれ具体例を置く。
- 決定: 選択肢2。講習会名は「例: なろう講習会第六回」、シリーズ名は「例: なろう講習会」とし、シリーズの一部では第何回かも講習会名へ書くよう説明する。
- 根拠: 2つの入力の粒度を、追加画面や説明カードを作らず既存フォーム上で区別できる。指定された文言をそのまま採用できる。
- 影響: 表示文言とplaceholderだけを変更する。既存の`WorkshopInput.title`と`OccurrenceInput.title`、D1カラム、API、複製動作は変更しない。新規CSS・依存はない。
- 再検討する条件: シリーズが独立エンティティとして検索・集約・継承を必要とする具体的運用が確定した場合。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/WorkshopWizardView.ts`、`app/ui/components/OccurrenceFields.ts`

## D-20260902-026 — 回番号の単発時入力とシリーズ名の任意性をラベルへ明示する

- 状態: superseded by D-20260902-031（回番号ラベルは継続）
- 判断が必要だった理由: 追加レビューで、単発講習会の回番号と、シリーズ名を入力する条件がラベルだけでは分かりにくいと確認された。
- 選択肢:
  1. 補足説明を別段落で追加する。
  2. 入力欄のラベル自体へ短い条件を加える。
- 決定: 選択肢2。「回番号（単発講習会なら1で）」「シリーズ名（あれば）」と表示する。
- 根拠: 入力時に必要な情報を対象欄の直近で確認でき、説明過多や新しいUIを避けられる。
- 影響: ラベルだけを変更し、回番号の既定値1、シリーズ名のnullable入力、API、D1、CSSは変更しない。
- 再検討する条件: 単発/複数回を先に選ぶ入力方式へ変更し、回番号を自動決定できるようになった場合。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/components/OccurrenceFields.ts`

## D-20260902-027 — プロフィールの3切替を下線型ARIAタブとして表現する

- 状態: decided
- 判断が必要だった理由: 追加レビューで、ARIA上はタブでもbasiQ-uiの塗り/枠付きボタン外観のため、ボタン切替に見えると確認された。
- 選択肢:
  1. basiQ-uiの`Button`へ`role=tab`を付けた現状を維持する。
  2. Tabs部品がない不足分だけ、native buttonを下線型タブとして実装する。
- 決定: 選択肢2。選択タブだけaccentの下線と文字色を付け、背景塗りと囲み枠をなくす。既存の`tablist`/`tab`/`tabpanel`、roving tabindex、左右矢印/Home/End操作は維持する。
- 根拠: タブの意味・見た目・キーボード操作が一致する。basiQ-uiにTabsはなく、既存部品を誤った外観で流用するよりbasiQ tokenだけを使う最小不足部品が妥当。
- 影響: D-20260902-021のbasiQ-ui`Button`外観採用部分を置換する。再試行など通常操作は引き続きbasiQ-uiの`Button`を使う。追加依存・汎用コンポーネントはない。
- 再検討する条件: basiQ-uiに正式なTabsが追加された場合は置換する。
- 参照: 本人指定のtraQレビュー（`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/ProfileView.ts`、`app/styles/pages.css`

## D-20260902-028 — 講習会を先に永続化し、保存済み画面で開催を追加する

- 状態: superseded by D-20260905-086
- 判断が必要だった理由: 追加レビューで、講習会名・概要の入力後に「次へ」と進むのではなく、その時点で講習会を作成・表示し、続いて各回を個別の枠で登録する流れが求められた。既存の公開条件と講習会/開催の独立モデルを壊さず、開催0件の中間状態を扱う必要があった。
- 選択肢:
  1. 3段階ウィザードの最終操作まで講習会と開催を一括保存する。
  2. POSTだけ開催0件を許可して講習会の器を先に保存し、保存済み通常編集画面へ遷移して最初の開催を追加する。PUTでは引き続き開催1件以上を必須にする。
- 決定: 選択肢2。新規画面は講習会名・概要とbasiQ-uiの「講習会を作成」ボタンだけを表示する。作成後は`/admin/workshops/{id}?created=1`で保存済み内容、成功通知、第1回の開催カードを表示する。公開開催がない講習会は管理用GETだけで読め、学習者向け一覧・詳細からは除外する。
- 根拠: 講習会と開催のデータ境界を画面上の操作と一致させ、作成直後から連番IDを持つ実データとして扱える。空の中間状態は非公開なので、未完成情報が学習者へ漏れない。
- 影響: D-20260902-021の「最初の開催まで3段階で登録」とD-20260902-025の「講習会名へ回数を書く」部分を置換する。講習会名の例は「なろう講習会」とし、シリーズならシリーズ名を書く。開催カードは回番号を見出しに反映し、従来の複製、公開状態、教材、対象者、関係保存を維持する。新規CSS・依存・migrationはない。
- 再検討する条件: 開催を作らず放置された講習会の整理、削除、監査が内部試用で必要になった場合。
- 参照: 本人指定のtraQレビュー（17:55、17:57、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/app/api/workshops/route.ts`、`app/lib/domain.ts`、`app/ui/views/WorkshopWizardView.ts`

## D-20260902-029 — 年度を日時から導出し、10件の代表seedで班と複数回表示を確認する

- 状態: decided
- 判断が必要だった理由: 追加レビューで年度の二重入力をなくし、開催組織・班を明示すること、少数fixtureでは確認できなかったグラフィックス班・ゲーム班・Unity全3回の表示を実データで確認することが求められた。
- 選択肢:
  1. 年度入力と公開3件のseedを維持する。
  2. 日時変更時に先頭4桁を年度へ同期して年度欄を除き、公開seedを10講習会へ増やす。
- 決定: 選択肢2。日時未定時は作成時の現在年を内部値として保持し、日時が入った時点でその年へ更新する。班は「開催する組織・班」と表示する。Unity講習会は一つの講習会の下に「Unity講習会第一回」「第二回」「第三回」の3開催を持たせ、グラフィックス班を含む他分野も1回開催として追加する。
- 根拠: 同じ情報の手入力不一致を避け、中心モデルの1回/複数回を10件のカード密度で評価できる。追加seedは`INSERT OR IGNORE`なので既存ローカルデータを上書きしない。
- 影響: 新規環境の学習者向け一覧は公開10件、下書き1件になる。年度のAPI/D1列は検索・過去資産の保持に必要なため残す。日時未定の下書きも従来どおり保存できる。新規CSS・依存はない。
- 再検討する条件: 年度が暦年ではなく会計年度・活動年度を意味すると運用上確定した場合、または本番データ移行時にfixtureを分離する場合。
- 参照: 本人指定のtraQレビュー（17:48、17:50、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/components/OccurrenceFields.ts`、`app/db/setup.ts`、`app/tests/api.integration.test.mjs`

## D-20260902-030 — バッジの日付を受講完了日と呼び、詳細導線を中央に置く

- 状態: decided（Lecture単位の完了日表示はD-20260904-060でsuperseded。詳細導線を設ける判断は継続）
- 判断が必要だった理由: push後の追加レビューで、「獲得日」では保存元の完了記録との関係が曖昧で、詳細導線の左寄せもバッジ詳細カード内で不均衡と確認された。
- 選択肢:
  1. 既存文言とfooter左寄せを維持する。
  2. 「受講完了日」へ変更し、既存の中央揃え共通レイアウトで詳細導線を中央配置する。
- 決定: 選択肢2。日付値と完了データは変更せず表示名だけを直し、footer内で既存`empty-actions`を再利用する。
- 根拠: バッジが講習会完了記録から導出される現在のモデルを正確に表現でき、専用CSSを増やさず指定配置にできる。
- 影響: `ProfileView`のバッジ詳細だけを変更する。ロードマップの同名導線、完了日時、API、D1は変更しない。新規CSS・依存はない。
- 再検討する条件: バッジ獲得条件が受講完了以外にも増え、獲得日時を別に保持する場合。
- 参照: 本人指定のtraQレビュー（18:02、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/ProfileView.ts`、`app/styles/base.css`

## D-20260902-031 — 開催のシリーズ名入力を廃止し、作成中の講習会をホームで更新確認できるようにする

- 状態: decided
- 判断が必要だった理由: 追加レビューで開催ごとのシリーズ名入力は不要とされ、講習会作成直後と保存のたびにホームで変更を確認し、未確定値は「未定」と示すことが求められた。一方、通常検索へ未公開情報を混ぜない境界も維持する必要があった。
- 選択肢:
  1. ホームと検索の両方を公開開催だけに限定し、作成中は管理画面でだけ確認する。
  2. ホーム取得だけに作成中を含め、更新日時順で表示する。通常検索と学習者向け詳細は公開開催だけに限定する。
- 決定: 選択肢2。`includeDrafts=1`をホーム専用で使用し、全開催からホーム用の班・年度・開催数を集約する。開催0件は「班 未定」「年度 未定」「開催未定」とし、未公開カードは`/admin/workshops/{id}`へ「編集を続ける」で戻す。開催フォームの任意`title`入力は削除するが、既存データと複製互換の列は削除しない。「学べること」はフォーム、詳細、検証文言を「この回で学べること」へ統一する。
- 根拠: 作成・保存結果をホーム先頭で即確認でき、未完成カードから編集を再開できる。公開検索と学習者詳細を分けるため、過去資産の発見品質や公開条件は崩れない。
- 影響: D-20260902-026のシリーズ名入力部分を置換する。Homeの件数には公開前講習会も含まれ、通常検索の件数は公開講習会だけを維持する。新規CSS・依存・migrationはない。
- 再検討する条件: 本番で運営者以外へ作成中講習会を見せない権限要件が確定した場合は、ホームの`includeDrafts`を認証済み管理者へ限定する。
- 参照: 本人指定のtraQレビュー（18:25、18:27、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/db/repository.ts`、`app/ui/views/HomeView.ts`、`app/ui/components/WorkshopSummaryCard.ts`、`app/ui/components/OccurrenceFields.ts`

## D-20260902-032 — 前後関係の複数選択を件数と明るい緑で明示する

- 状態: decided
- 判断が必要だった理由: 追加レビューで、「先に学ぶ」「次に学ぶ」を同時に1件以上選べないように見え、選択状態も背景が薄すぎて判別しづらいと確認された。実ブラウザでは両側の複数選択自体は成立していたため、モデルを広げる変更ではなく操作結果の可視化が必要だった。
- 選択肢:
  1. basiQ-ui `ToggleButton`の既定on-stateだけを維持する。
  2. 両見出しへ選択件数を表示し、選択行と実コンポーネントのon-stateを既存の成功色tokenへ対応させる。
- 決定: 選択肢2。両側は独立した配列として複数選択を維持し、重複IDを除く。同一講習会を前後へ同時指定する操作だけは直接の循環になるため、従来どおり反対側から外す。
- 根拠: 2件+1件の同時選択と保存結果をその場で確認でき、独自トグルや新しい色を作らずbasiQ-uiの状態変数と既存の完了色でレビュー意図を満たせる。
- 影響: `WorkshopWizardView`、ドメインテスト、API統合テストを更新する。新規CSSセレクタ、依存、migration、保存形式の変更はない。循環関係のAPI拒否も維持する。
- 再検討する条件: 同じ講習会を前後へ同時指定することに循環以外の運用上の意味が確定した場合、またはbasiQ-uiに成功toneのToggleButtonが追加された場合。
- 参照: 本人指定のtraQレビュー（18:32、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/WorkshopWizardView.ts`、`app/lib/domain.ts`

## D-20260902-033 — 講習会削除は影響範囲を確認してから関連データごと実行する

- 状態: superseded by D-20260904-062
- 判断が必要だった理由: 追加レビューで編集画面の保存ボタン横に赤い講習会削除ボタンが求められた。過去講習会を資産として残す方針と、誤操作で開催・完了・ロードマップ関係まで失う危険を両立して扱う必要がある。
- 選択肢:
  1. 削除ボタンを表示するが、下書きだけに制限する。
  2. 明示的な管理操作として全講習会を削除可能にし、影響する関連データを確認文へ列挙して取消機会を置く。
- 決定: 選択肢2。basiQ-ui `Button`のdanger toneを使い、開催、完了記録、ロードマップ上の配置も消えることと不可逆性をnative confirmで知らせる。削除中は保存と削除を無効化し、成功後はホームへ戻す。
- 根拠: レビューで求められた管理操作を実データへ接続しつつ、通常の編集・保存と破壊操作を色、文言、確認で分離できる。D1の既存`ON DELETE`制約を使うため、部分削除や孤児レコードも残らない。
- 影響: `DELETE /api/workshops/{id}`、repository、UI API、編集画面、API統合テストを追加する。過去資産の通常表示・発見は維持されるが、管理者が確認を承諾した講習会は関連記録ごと削除される。新規CSS、依存、migrationはない。
- 再検討する条件: 認証・権限を実装する時点で、物理削除を管理者専用のアーカイブ/復元へ置き換える要件が確定した場合。
- 参照: 本人指定のtraQレビュー（18:38、18:41、18:48、18:50、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/WorkshopWizardView.ts`、`app/db/repository.ts`

## D-20260902-034 — 開催削除は共通保存の差分として扱い、最後の1件を保護する

- 状態: superseded by D-20260904-062
- 判断が必要だった理由: 追加レビューで開催カードごとの「−」削除が求められた。専用削除APIを増やすか、講習会/開催を同時編集する既存PUTの意味を完成させるか、また開催0件を通常更新で許すかを決める必要があった。
- 選択肢:
  1. 開催専用DELETE APIを追加し、各ボタンから即時削除する。
  2. フォームから除外した開催を講習会PUT時の差分として削除し、最後の1件だけはUIとドメイン制約の両方で保護する。
- 決定: 選択肢2。各カードにbasiQ-ui danger outlineの「−」を置き、2件以上のときだけ操作可能にする。repositoryは入力に残らない既存開催を同じD1 batchで削除し、他開催・関係の保存と原子的に反映する。
- 根拠: ウィザードと通常フォームが同じ講習会/開催データへ保存される設計を維持でき、削除だけ先に確定して保存失敗時に部分反映されることを避けられる。単発は第1回だけというレビュー文言とも一致する。
- 影響: `OccurrenceFields`、`WorkshopWizardView`、repository、API統合テストを更新する。専用API・依存・migrationはない。ホームカードの高さずれにはbasiQ-ui Cardを行高へ伸ばす共通レイアウト規則1件だけを追加し、独自外観は作らない。
- 再検討する条件: 開催単位の監査・復元・権限や、開催0件で講習会を維持する運用が確定した場合。
- 参照: 本人指定のtraQレビュー（19:06、19:08、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/components/OccurrenceFields.ts`、`app/ui/components/WorkshopSummaryCard.ts`、`app/db/repository.ts`

## D-20260902-035 — 編集の初期読込失敗時はフォームを隠して再試行へ限定する

- 状態: decided
- 判断が必要だった理由: 追加レビューで、サーバーへ接続できないときに未取得の編集情報を表示せず、再試行だけを提示することが求められた。保存中の失敗とは異なり、初期読込失敗では正しい編集対象を構成できない。
- 選択肢:
  1. 空フォームと失敗feedbackを同時に表示し、利用者が入力を始められるようにする。
  2. 初期読込失敗を専用状態にし、フォーム全体を隠して再試行だけを出す。
- 決定: 選択肢2。`loadFailed`を読込中・保存失敗と分け、同じ`load`を呼ぶbasiQ-ui neutral outlineの再試行ボタンを表示する。開催追加は記号だけのoutlineからaccent solidの「開催データを追加」へ置換する。
- 根拠: 未取得情報を正しい値に見せて上書きする事故を防ぎ、利用者の次の操作を明確にできる。basiQ-uiの既存feedback/Button状態で実現できる。
- 影響: `WorkshopWizardView`と、モバイルで追加ボタンの文言を保つレイアウト規則1件を更新する。保存失敗時は入力保持を維持し、初期読込失敗時だけフォームを隠す。新規依存・migrationはない。
- 再検討する条件: オフライン編集やローカルキャッシュからの復元を、データ版管理付きで実装する場合。
- 参照: 本人指定のtraQレビュー（19:13、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/WorkshopWizardView.ts`

## D-20260902-036 — 保存後は公開状態にかかわらず管理用詳細previewで確認する

- 状態: decided
- 判断が必要だった理由: 追加レビューで、「変更を保存」後に詳細を直接確認し、ホームの「編集を続ける」も「詳細を見る」へ変えることが求められた。一方、下書きを通常の学習者向け公開APIへ混ぜずに詳細表示する必要があった。
- 選択肢:
  1. 保存時に開催を自動公開し、通常詳細へ遷移する。
  2. 公開状態は利用者入力のまま維持し、`manage=1`の管理用詳細previewへ遷移する。
- 決定: 選択肢2。保存成功後は`/workshops/{id}?manage=1&saved=1`へ遷移し、成功statusと保存済み内容を表示する。公開前ホームカードも`/workshops/{id}?manage=1`へ「詳細を見る」でつなぐ。
- 根拠: 下書きを意図せず公開せず、保存結果を詳細レイアウトで即確認できる。詳細には既存の編集リンクがあるため、確認と再編集を一巡できる。
- 影響: `WorkshopWizardView`、`WorkshopSummaryCard`、`WorkshopDetailView`を更新する。公開前previewでは不整合な完了記録を作らないよう学習状況カードを隠す。通常検索・公開詳細API・公開済み完了動作は変更しない。新規CSS・依存・migrationはない。
- 再検討する条件: 認証・権限を実装し、管理preview URLを権限で保護する時点、または保存と公開を同一操作にする運用が確定した場合。
- 参照: 本人指定のtraQレビュー（19:25、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/views/WorkshopWizardView.ts`、`app/ui/views/WorkshopDetailView.ts`、`app/ui/components/WorkshopSummaryCard.ts`

## D-20260903-037 — 開催削除後は回グループを再採番し、ロードマップ完了だけを成功色で示す

- 状態: decided
- 判断が必要だった理由: 追加レビューで、第1回削除後に第2回だけが残る番号飛び、ロードマップの現在地線と円マーカーの重なり、全件完了後も青い選択色のままになる表示が確認された。
- 選択肢:
  1. 回番号は利用者入力のまま残し、ロードマップの既存accent表現も維持する。
  2. 削除操作の時点だけ残存回を1から再採番し、現在地は青、完了は既存成功色という意味分けを画面全体で揃える。
- 決定: 選択肢2。開催は同じ`sequenceNumber`を持つ通常開催・再放送を1グループとして連番へ写像する。ロードマップ経路は行を12px内側へ置いて左のaccent線とマーカーを分離し、完了ロードマップの一覧行とbasiQ-ui `Card`は`--app-success`、`--app-success-soft`および`--basiq-color-card-background`で緑表示する。
- 根拠: 再放送を回の中で区別する設計を壊さず、人向け番号の欠番を防げる。青を現在地・主操作、緑を完了に限定する既存β方針とも一致し、Card自体の描画はbasiQ-ui tokenに委ねられる。
- 影響: `workshopForm`、`WorkshopWizardView`、`WorkshopListView`、ロードマップ既存CSS、ドメイン/APIテストを更新する。回番号の手入力、内部開催ID、コピー元、永続化形式、依存、migrationは変更しない。
- 再検討する条件: 回番号を任意の章番号として欠番込みで保持する運用、またはbasiQ-uiにsuccess toneのCard/進捗部品が追加された場合。
- 参照: 本人指定のtraQレビュー（2026-09-03 00:01〜00:04、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、読み取り専用）、`app/ui/forms/workshopForm.ts`、`app/ui/views/WorkshopListView.ts`、`app/styles/pages.css`

## D-20260903-038 — ロードマップの状態変更を保存結果で通知し、一覧行を詳細リンクにする

- 状態: decided
- 判断が必要だった理由: 追加レビューで、ロードマップの公開/非公開を保存した結果が画面上で分からず、学習者向け一覧行も選択プレビューの更新に留まって詳細へ進むには別のリンクが必要だと確認された。
- 選択肢:
  1. 公開トグルの表示と右プレビュー内の詳細リンクだけを維持する。
  2. 最後に保存された公開状態と保存結果を比較して公開/非公開を色分け通知し、一覧行自体を詳細URLを持つリンクにする。
- 決定: 選択肢2。公開保存は既存`feedback-success`、非公開保存は既存`feedback-error`を`role=status`で表示する。一覧行はsemanticな`a`へ変更し、hover/focus時の右プレビュー更新と完了色は維持する。
- 根拠: 保存された外部状態を明確に伝え、一覧の主要対象から直接詳細へ進める。新しい通知部品や外観を作らず、既存の状態表現とレイアウトだけで実現できる。
- 影響: `RoadmapEditorView`、`WorkshopListView`、一覧要素を`button`から`a`へ変える既存CSSセレクタを更新する。API、D1、公開条件、依存、migrationは変更しない。traQ参照は本人が明示承認した最新投稿への`:eyes:`1件だけを例外とし、投稿・編集・その他のリアクションは行っていない。
- 再検討する条件: 保存後に一覧/詳細へ自動遷移する運用へ変える場合、またはbasiQ-uiに標準Alert/Tabs/LinkCardが追加された場合。
- 参照: 本人指定のtraQレビュー（2026-09-03 00:35〜00:41、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`）、`app/ui/views/RoadmapEditorView.ts`、`app/ui/views/WorkshopListView.ts`、`app/styles/pages.css`

## D-20260903-039 — traQの確認境界はeyesスタンプとし、チャンネルAPIを直接ポーリングする

- 状態: decided（トークン取得方法はD-20260903-042でKeychainへ拡張）
- 判断が必要だった理由: 認証済みChromeを定期操作する方式はDOM・画像処理と毎回の文脈読込が重く、WebSocket Botは10分程度の遅延を許容する用途に常時接続管理を持ち込む。状態ファイルを別途持たず、複数の新規投稿も一度に取得できる境界が必要だった。
- 選択肢:
  1. Chromeで閲覧し、画面上の最新投稿を毎回判定する。
  2. WebSocket Botを常駐させ、投稿イベントを受信する。
  3. Botトークンで`GET /channels/{channelId}/messages`を定期実行し、`:eyes:`を処理済み境界に使う。
- 決定: 選択肢3。降順でメッセージをページングし、最初の`:eyes:`より新しい全投稿を古い順に出力する。新規があれば最新投稿だけにBot名義で`:eyes:`を1個付ける。どの利用者が付けた`:eyes:`でも確認済み境界とし、0件時は書込APIを呼ばない。
- 根拠: チャンネルメッセージAPIの`Message`に`stamps`が含まれるため追加のスタンプ照会や検索サービスが不要で、最新への`:eyes:`がリモート側の冪等cursorになる。次回はその投稿で走査を止められ、常駐プロセスもローカル状態同期も要らない。
- 影響: トークンは`TRAQ_BOT_TOKEN`または`TRAQ_ACCESS_TOKEN`を優先し、なければD-20260903-042のKeychainから読み、ログへ出さない。既定パスはZennチャンネルだが、チャンネル/eyes UUIDを環境変数で固定すれば定期実行時のAPI呼出を2回減らせる。`--dry-run`では一切書き込まず、`--quiet`では投稿内容を標準出力へ出さない。本人から最新投稿への`:eyes:`は都度承認不要という継続承認を得ており、CLI以外の投稿・編集・リアクションは行わない。新規依存はない。
- 再検討する条件: 即時処理が必要になる、`:eyes:`を人間の通常リアクションと区別する必要が生じる、Botに過去メッセージ取得権限がない、または複数ワーカーが同時実行される場合。専用スタンプ、永続message ID、WebSocketイベントの順で再検討する。
- 参照: 本人依頼、traQ公式OpenAPI `GET /channels/{channelId}/messages`・`GET /stamps`・`POST /messages/{messageId}/stamps/{stampId}`、`app/scripts/traq-zenn-feedback.mjs`

## D-20260903-040 — ロードマップをMarkdownで共有し、複数開催の教材を各開催へ戻す

- 状態: decided（共有と配置判断は継続。教材のデータ型はD-20260905-089でsuperseded）
- 判断が必要だった理由: 追加レビューで、ロードマップを部員へ渡すための再利用可能な形式と、複数開催でどの教材がどの回に属するかをカード内から直接判別できる導線が求められた。D-20260902-022の教材一本化は重複削減には有効だったが、複数回では開催と教材の対応を離していた。
- 選択肢:
  1. 既存の画面URLだけを共有し、教材は全開催分を独立した節へ集約したままにする。
  2. ロードマップの段階・講習会名・絶対URLをMarkdown化してコピーできるようにし、複数開催だけは教材を該当開催カード内へ配置する。
- 決定: 選択肢2。ロードマップ見出し横のbasiQ-ui `Button`で共有欄を開き、readonly Markdownとコピーボタンを表示する。複数開催は各カード内に「教材はこちら」を置き、再放送ラベルは見出し右端に置く。1回開催は階層を隠す原則を優先して従来の統合教材節を維持する。
- 根拠: 共有文はtraQ等へそのまま貼れ、URLは名称変更の影響を受けない連番IDを使う。教材の所在を開催データ境界と一致させつつ、単発画面へ不要なカード階層や重複を戻さない。
- 影響: D-20260902-022の教材配置判断を複数開催に限り置換する。`RoadmapDetailView`、`WorkshopDetailView`と既存レイアウト規則を更新する。API、D1、入力データ、依存、新規外観CSSは変更しない。
- 再検討する条件: traQ向け以外の共有形式、公開URLの正式origin、またはbasiQ-uiに標準Clipboard/Share部品が追加された場合。
- 参照: 本人指定のtraQレビュー（2026-09-03 00:47〜00:49、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、閲覧のみ）、`app/ui/views/RoadmapDetailView.ts`、`app/ui/views/WorkshopDetailView.ts`

## D-20260903-041 — β fixtureはseed markerがない初回だけ投入する

- 状態: decided
- 判断が必要だった理由: 起動のたびに固定IDのfixtureを`INSERT OR IGNORE`していたため、利用者がロードマップ段階を編集したD1では固定段階IDの挿入が無視され、そのIDを参照するitem挿入だけが外部キー制約で失敗した。また、利用者が削除したfixtureを再起動時に復活させる可能性もあった。
- 選択肢:
  1. 各fixture SQLをさらに冪等化し、毎起動で期待形へ戻す。
  2. migrationで一度限りのseed markerを持ち、markerがない新規環境だけfixtureを同一batchで投入する。既存βデータがある環境はmigrationでmarkerをbackfillする。
- 決定: 選択肢2。`beta_seed_state`へ`beta-v1`を記録し、存在しない場合だけβ fixtureとmarker挿入を同じD1 batchへ含める。既存β講習会がある環境はmigration時点でmarkerを作る。
- 根拠: 初期fixtureはアプリの正本ではなく新規ローカル環境用であり、起動時の期待形復元より利用者の編集・削除を守る方が「過去の講習会を資産として残す」目的に合う。同一batchなら初回投入が途中状態にならない。
- 影響: `0003_beta_seed_state.sql`、migration registry、schema、setup、API統合テストを更新する。既存テーブルやデータは削除せず、新規環境の代表fixtureは維持する。新規依存はない。
- 再検討する条件: fixtureを明示コマンドでリセットする開発運用、バージョン別の追加fixture配布、または本番データ移行と開発seedを完全分離する時点。
- 参照: 本人指定のtraQレビュー（2026-09-03 01:06、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、閲覧のみ）、`app/drizzle/0003_beta_seed_state.sql`、`app/db/setup.ts`、`app/tests/api.integration.test.mjs`

## D-20260903-042 — Cron用BotトークンはmacOS Keychainから実行時に取得する

- 状態: decided
- 判断が必要だった理由: 10分heartbeatからCLIを無人実行するには資格情報が必要だが、トークンをCronプロンプト、Git管理ファイル、シェル履歴、通常ログへ保存してはならない。
- 選択肢:
  1. Cronプロンプトまたは`.env`へ平文保存する。
  2. macOS Keychainへ保存し、CLIが環境変数未指定時だけ`security`経由で読む。
  3. 毎回利用者へ入力を求める。
- 決定: 選択肢2。serviceを`codex.1-monthon.traq-bot-token`、accountを`1-monthon`へ固定し、既存heartbeat `zenn`の先頭処理を`npm run feedback:check -- --json`へ置換する。新規0件なら他の処理を行わない。
- 根拠: Keychainはリポジトリとタスク本文から秘密を分離しながら、同じローカルMac上のスケジュール実行から取得できる。読み取り専用quiet dry-runで実API接続まで確認できた。
- 影響: CLIは環境変数、Keychainの順で資格情報を探す。heartbeatはtraQ確認にChromeを使わず、CLI失敗時もChromeへフォールバックしない。ローカルUIの変更後QAには従来どおりChromeを使う。秘密値は成果物と自動化設定に含めない。
- 再検討する条件: 実行ホストをmacOS以外へ移す、Keychainアクセスが無人実行で拒否される、またはCodexのローカルスケジュール済みタスクに暗号化Secret注入機能が提供される場合。
- 参照: 本人依頼、OpenAI Docs「スケジュール済みタスク」、`app/scripts/traq-zenn-feedback.mjs`、automation `zenn`

## D-20260903-043 — 追加fixtureは非公開情報を一般化し、既存IDと分離する

- 状態: decided
- 判断が必要だった理由: Zennの追加レビューで参照テキストを基にmock dataを増やす要望があった。一方、添付には個人名、正確な日程・場所、内部URLが含まれ、既存ローカルD1には利用者が編集した講習会・ロードマップ段階があるため、そのままの転載と固定小番号の再利用は避ける必要があった。
- 選択肢:
  1. 添付内容を忠実にfixture化し、既存seedの直後の小さい連番を使う。
  2. 班、学習分野、開催回数、一般的な順序だけを抽出し、公開資料URL以外を除いた一般化fixtureを追加migrationで一度だけ投入する。fixture IDは既存試用データと離した専用範囲に置く。
- 決定: 選択肢2。17講習会・40開催・4ロードマップを追加し、講習会は`260012`以降、ロードマップは`270002`以降、段階は`280003`以降のfixture専用範囲を使う。元のfixtureを先に投入し、追加migrationはD1の制約に合わせて再実行可能な文単位で適用し、全成功後だけledgerへ記録する。
- 根拠: 表示URLは名称ではなく連番BIGINTを使う既存方針を守りつつ、既存利用者データとの衝突と、編集中の段階IDへ追加itemが混入する事故を避けられる。内部情報を再配布せず、検索、複数回、班別ロードマップの品質確認に必要な多様性だけを得られる。
- 影響: 新規環境は公開27講習会、下書き込み28講習会、5ロードマップになる。新規作成IDはfixture範囲以降へ進むが、IDは内部識別子であり表示上の回番号とは分離される。D-20260903-041の「利用者による編集・削除を再起動で復活させない」判断は維持し、今回のカタログはmigration ledgerで一度だけ追加する。新規依存はない。
- 再検討する条件: 本番用seedと開発fixtureを別コマンドへ分離する、既存本番IDの予約規則が決まる、またはfixtureを任意に導入・削除する仕組みを作る場合。
- 参照: 本人指定のtraQレビュー（2026-09-03 01:39まで、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、CLI閲覧のみ）、読み取り専用の添付テキスト、`app/drizzle/0004_extended_beta_catalog.sql`

## D-20260903-044 — 公開状態の保存結果を画面先頭で示し、学習順の線を途切れさせない

- 状態: decided（経路線の終端・重なり順はD-20260903-046でsuperseded。保存通知の判断は維持）
- 判断が必要だった理由: 長いロードマップ編集画面では、下部の保存操作後に上部へ出る公開/非公開通知が見えないことがあった。また、学習順の縦線が各項目の境界で僅かに途切れ、順序の連続性が弱く見えた。
- 選択肢:
  1. 通知位置を下部へ複製し、線は各行で独立したままにする。
  2. 公開状態が変わった保存だけ既存status更新後に画面先頭へ戻し、既存疑似要素の終端だけを次の項目へ接続する。
- 決定: 選択肢2。Vueの`nextTick`後に`window.scrollTo({ top: 0, behavior: 'smooth' })`を呼び、成功/非公開の既存feedbackを確実に見せる。縦線は既存basiQ border tokenを保持し、`bottom`だけを`-20px`へ調整する。開催カード内教材リンクは変更せず回帰確認する。
- 根拠: 同じ通知を二重表示せず主操作の結果へ視線を戻せる。線の色・太さ・カード外観を新設せず、情報の順序だけを明確にできる。
- 影響: 公開/非公開変更を保存するとスクロール位置が先頭へ移る。通常の内容保存では従来動作を維持する。新規コンポーネント、依存、外観CSSセレクタは追加しない。
- 再検討する条件: basiQ-uiへtoast/notification centerが追加される、保存操作を画面上部へ移す、または利用者テストで自動スクロールが編集継続を妨げると判明した場合。
- 参照: 本人指定のtraQレビュー（2026-09-03 01:39まで、同URL、CLI閲覧のみ）、`app/ui/views/RoadmapEditorView.ts`、`app/styles/pages.css`

## D-20260903-045 — 小さな補助操作はbasiQ-ui outlineとボタン内状態変化で示す

- 状態: decided
- 判断が必要だった理由: 追加レビューで、開催カードの青い教材操作が本文より強く見えることと、Markdownコピー成功feedbackが後続内容へ覆いかぶさることが指摘された。
- 選択肢:
  1. 独自CSSで教材リンクとコピー通知の色・位置を個別調整する。
  2. 教材を実basiQ-ui neutral outlineボタンへ置き換え、コピー成功は同じボタンのvariantと文言を変更して示す。失敗時だけ既存error feedbackを出す。
- 決定: 選択肢2。教材ボタンは既存`section-heading`の右端に置く。コピー前はaccent solidの「Markdownをコピー」、成功後はneutral outlineの「コピーしました」とし、独立した成功feedbackを表示しない。
- 根拠: 主操作と補助操作の強弱がbasiQ-uiの既定variantで揃い、追加の色指定や位置指定を作らずに意図した見た目と状態伝達を実現できる。成功結果を操作元へ集約するため、後続内容を押し下げたり重ねたりしない。
- 影響: 複数開催の教材はリンク要素から新しいタブを開くボタンへ変わるが、キーボード操作と実URLは維持する。Clipboard失敗時はテキスト選択を促すerror feedbackを維持する。新規CSS、依存、API変更なし。
- 再検討する条件: basiQ-uiに外部リンクボタン、ClipboardButton、toastが追加される、または新規タブを開く操作をリンクへ統一するアクセシビリティ方針が決まる場合。
- 参照: 本人指定のtraQレビュー（2026-09-03 01:48〜01:57、同URL、CLI閲覧のみ）、`app/ui/views/WorkshopDetailView.ts`、`app/ui/views/RoadmapDetailView.ts`

---

## D-20260903-046 — 開閉文言と経路線を実状態に一致させ、テーマ境界で色を解決する

- 状態: decided
- 判断が必要だった理由: 内部レビューで共有欄の開閉操作、完了マーカー間の線、複数開催の表記が分かりにくいと指摘された。Chrome確認では、固定ライトテーマの外側で派生色を計算するとOSダーク設定の色が混入し、現在地の文字も読みづらくなると判明した。
- 選択肢:
  1. 閉じる操作を別に足し、完了状態用の専用線と固定背景色を追加する。
  2. 既存basiQ-uiボタンの文言を状態連動させ、同じ線の重なり順を補正し、既存派生トークンをThemeProviderの内側で計算する。
- 決定: 選択肢2。共有欄を開くと同じボタンを「共有画面を閉じる」とし、`aria-expanded`とEnter操作を維持する。経路線は行背景より上、マーカーより下へ置き、マーカー中心間を結ぶ。共通カードは既存開催件数のまま「全N回」と表示し、0件の「開催未定」と1件の「1回完結」は変えない。accent派生色の定義を`:root`から`.theme-root`へ移す。
- 根拠: D-20260903-044の終端延長だけでは隣の完了・現在地行の背景が線を隠すため、描画順の補正が必要。basiQ-uiの色はThemeProviderのスコープで解決することで、固定色や新規外観CSSを追加せず読みやすさを保てる。
- 影響: D-20260903-044の線配置だけを置換する。API、D1、開催/回番号の数え方、コピー元、公開・完了記録は変更しない。新規CSSセレクタ、依存、部品は追加しない。確認操作は隔離D1だけで行う。
- 再検討する条件: basiQ-uiにTimeline/Disclosure部品が追加された場合、または再放送を除いた回数と実開催件数を一覧で区別する運用が確定した場合。
- 参照: 本人指定のtraQレビュー（2026-09-03 02:28〜02:38、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、CLI参照）、`app/ui/views/RoadmapDetailView.ts`、`app/ui/components/WorkshopSummaryCard.ts`、`app/styles/pages.css`、`app/styles/base.css`

---

## D-20260903-047 — 編集操作は実ボタンの内容幅を保ち、削除の有無で列をずらさない

- 状態: decided
- 判断が必要だった理由: 作成画面は削除ボタンがないため、4列gridの2列目へキャンセルが入り過大に伸びていた。モバイルの均等2列・幅100%指定も内容幅を失わせていた。主見出し上の管理ラベルも重複していた。
- 選択肢:
  1. 作成/編集ごとのgrid列数とキャンセルの固定px幅を追加する。
  2. 実basiQ-uiボタンの内容幅を使い、キャンセル・保存を同じ操作グループにして折返し可能な配置へ変える。
- 決定: 選択肢2。キャンセルをneutral outlineの`BasiqButton`へ統一し、通常時は運営向けページへ戻す。保存・削除中は他操作と同様に無効化する。操作グループは既存`admin-row-actions`を使い、外側だけflex配置へ変更する。「ロードマップ管理」は作成/編集の両方から除く。
- 根拠: 固定幅を増やさず、basiQ-uiの文字幅・余白で保存より少し長いキャンセルを得られる。390pxでは削除と補助/保存の組を別行へ折り返し、二つの主要ボタンを分離しない。
- 影響: 表示と操作配置のみ。API、D1、入力、保存・公開規則は維持。外観CSS、新規依存、新規コンポーネントは追加せず、既存の均等幅ルールを除去する。
- 再検討する条件: 編集操作の常時固定をやめる、またはbasiQ-uiに標準ActionBarが追加された場合。
- 参照: 本人指定のtraQレビュー（2026-09-03 02:41、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、CLI参照）、`app/ui/views/RoadmapEditorView.ts`、`app/styles/pages.css`

---

## D-20260903-048 — 標準フォントを自己配信し、next更新の安全制限は維持する

- 状態: フォント判断はdecided。next更新を待つ判断のみD-20260903-049によりsuperseded。
- 判断が必要だった理由: 内部レビューでbasiQ-uiのnext更新と標準フォントの適用が求められた。現行版の`--basiq-font-family-sans`はInter/M PLUS 1pを指定するが、アプリはフォントファイルを読み込んでいなかった。
- 選択肢:
  1. 外部フォントCDNと独自のfont-family上書きを追加する。
  2. 指定Fontsourceパッケージを固定し、basiQ-uiの既存トークンをそのまま機能させる。
- 決定: 選択肢2。`@fontsource/inter@5.3.0`と`@fontsource/m-plus-1p@5.3.0`を正確な版・integrityでlockfileへ固定し、normalの400/500/700/800/900を共通CSS入口で読み込む。Interはlatinのみ、M PLUS 1pは日本語を含む配布元のunicode-range分割を利用し、必要な文字のファイルをブラウザが取得する。既存の650/680/750/850指定はCSS標準の近傍ウェイト選択を使い、外観ルールやトークンは追加・上書きしない。
- 根拠: 配布元README、metadata、LICENSEとbasiQ-uiの既存tokenを確認。両フォントはOFL-1.1、追加の推移依存・install scriptなし。著作権表示とOFL全文を`app/public/font-licenses.txt`へ同梱し、ビルド後も配信する。外部CDNや認証情報は不要。
- nextの扱い: npm registryの`next`は`0.1.0-beta.3`、`latest`は`0.1.0-beta.0`。beta.3の通常取得は環境の最小公開経過時間制限によりETARGETとなった。別URL取得、設定上書き、旧nextへの勝手な変更は行わず、本体と既存vendorはbeta.0のまま維持する。D-20260826-004の版固定とD-20260826-009の安全制約を継続し、supersededにはしない。
- 影響: フォント読込だけを追加し、Vue/basiQ-ui実部品、入力、API、D1、既存試用データを維持する。初回のフォント配信容量は増えるが、`font-display: swap`とunicode-rangeを配布元のまま用いる。本番依存監査は0件。開発専用の既存fast-uri high 1件とdrizzle-kit系moderate 4件は別の依存保守対象とし、今回の2パッケージとは無関係である。
- 再検討する条件: 環境がbeta.3の通常取得を許可した時点でnextを再解決し、API/トークン差分と全リリースゲートを確認する。本体更新は完了扱いにしない。
- 参照: 本人指定のtraQレビュー（2026-09-03、`https://q.trap.jp/channels/event/1-Monthon/26/13/Zenn`、CLI参照）、npm公開メタデータ、`app/node_modules/basiq-ui/dist/styles.css`、Fontsource両パッケージのREADME/metadata/LICENSE

---

## D-20260903-049 — 本人指定のbasiq-uiだけを公開経過時間の例外にしてbeta.3へ更新する

- 状態: decided。D-20260903-048のnext更新待ちをsupersedeし、フォントの自己配信と版固定は継続する。
- 判断が必要だった理由: 本人から`minReleaseAgeExclude`へbasiQ-uiを追加する明示依頼があった。既定npm 11.16はこの設定に未対応だったため、制限を一律解除せず正式なパッケージ単位例外を扱う実行環境が必要だった。
- 選択肢: 全依存の公開後待機を無効化する／basiq-uiだけを除外して対応版npmを一時実行する。
- 決定: 後者。`app/.npmrc`に`min-release-age=3`と`min-release-age-exclude[]=basiq-ui`を置く。グローバルnpmと利用者の`.npmrc`は変更しない。pack時の除外適用にも対応するnpm 11.19.1（Artistic-2.0）を`npm exec`で一時利用し、プロジェクトはnpm 11.19以上を要求する。他パッケージと推移依存の3日制限を保つ。
- 根拠: npm公式設定とリリース記録、実行時の設定値を確認。正規registryから`npm pack --ignore-scripts`でbeta.3を取得し、配布物のMIT LICENSE、install hookなし、integrity一致を確認した。SHA-512は`sha512-Igl+D7X2YXzeV1vNCOgDGLTvASicbmF5o9liqJXU3k4FS8YtHsQHS8yIi9OG3V//Jw5HvYIgdYUv+KqQQNXKTw==`。
- 互換性: 公開ソース差分と配布宣言を比較。利用中の6部品は実装変更なし、型宣言5件同一、ToggleButtonのみ同じ型のプロパティ順が変わる。既存トークンの削除は0件、exportsは17件へ追加された。アプリが現在使うAPIに破壊的変更は認めず、UIソースとCSSの補修は不要と判断した。新しいInput/FormField/Tabs等の採用は別のUI変更単位で扱い、今回の依存更新へ混在させない。
- 影響: `app/vendor/basiq-ui-0.1.0-beta.3.tgz`を追加し、manifest/lockfileを固定。旧vendorは保持。Vue/reka-uiや他の依存は変更せず、API/D1/入力・完了記録も維持する。全ゲートとPC/390px操作確認が成功、本番依存監査は0件。旧npmを起動元にした場合だけ未対応設定の警告が出るためREADMEに一時実行方法を記載した。
- 再検討する条件: 安定版更新、basiQ-ui新部品への画面移行、または本人がこの限定例外を取り消す場合。安全制限を変更できないとした以前の説明は広すぎたため、本決定で訂正する。
- 参照: 本人の本タスクでの明示依頼、[npm設定](https://docs.npmjs.com/using-npm/config/#min-release-age-exclude)、[npm 11.19.0](https://github.com/npm/cli/releases/tag/v11.19.0)、[basiQ-ui差分](https://github.com/traP-jp/basiq-ui/compare/v0.1.0-beta.0...v0.1.0-beta.3)。非公開投稿本文は転載しない。

## D-20260904-050 — Lecture/Sessionを正本とし、案内資産を3属性のFlowとして管理する

- 状態: decided（Flowを直接対象へ結び付けるデータ構造はD-20260904-054でsuperseded。3属性と対象制約は継続）
- 判断が必要だった理由: 講習会と開催の境界を添付仕様へ揃え、従来の「ウィザード」がどの時点・対象を案内するものかを構造として区別する必要がある。
- 選択肢: 従来名と単一登録画面を維持する／Flowへ改称し対象別の属性を持たせる。
- 決定: 後者。`Lecture -> Session`を正本とし、Flowは`lecture_pre`（講習会の事前）、`session_main`（各開催のメイン）、`lecture_post`（講習会の事後）の3属性とする。事前・事後はLectureを、メインはSessionを必ず1件だけ参照する。
- 根拠: 複数開催を持つ講習会で、全体の準備・振り返りと当日の進行を重複なく管理できる。属性と対象をDB制約とAPI検証の双方で保証できる。
- 影響: `LECTURE_SESSION_FLOW_SPEC.md`を正本として追加し、Flowのmigration、型、検証、CRUD API、運営画面を追加する。既存ローカルデータは破壊せず、互換用の`beta_workshops`/`beta_occurrences`を当面保持する。画面・新規実装では「ウィザード」を使用しない。
- 再検討する条件: Flowの質問・フェーズ・版管理を実装する段階、または互換テーブルをLecture/Sessionの物理名へ切り替える移行時。
- 参照: 本人指定の`/Users/soma/Downloads/lecture_session_spec_v2.md`と、本タスクでの3属性指定。

## D-20260904-051 — β実装を継承せず、採用判断だけを本番コンテキストへ戻して新規構築する

- 状態: decided
- 判断が必要だった理由: 本番の技術選定がβ版と大きく異なり、βコードを段階的に改修すると、検証用の構造・互換層・依存関係が本番設計を拘束する。
- 選択肢: βコードを移行元として改修する／βを検証資料として固定し、採用する差分だけを意思決定へ変換して本番を新規構築する。
- 決定: 後者。β版のコード・DB・ルーティング・コンポーネント構成は本番の実装基盤にしない。人間がβへ入れた変更は、解決した問題、採用する挙動、根拠、適用条件に変換し、統合仕様と画面設計へ戻す。本番はその確定成果物と本番技術選定からフロムスクラッチで構築する。
- 根拠: βで得た利用上の知見を保持しつつ、Vue/Vinext/D1や`Workshop/Occurrence`互換層などの試作用制約を本番へ持ち込まずに済む。
- 影響: β版は動作参照・比較・判断抽出のため読み取り対象として凍結する。次の実装前に、仕様統合、β差分の採否、画面設計、人間レビューを完了させる。本番リポジトリの作成や技術選定は別チェックポイントで行う。
- 再検討する条件: βの一部に本番技術選定と完全に一致し、独立移植できる成果物が見つかった場合。その場合もコードベース全体ではなく、採用理由と検証方法を定めた小さな単位で取り込む。
- 参照: 本人の本タスクでの明示方針、β版の実装・レビュー記録。

## D-20260904-052 — βの画面知見を本番仕様から分離して画面設計の比較材料にする

- 状態: decided
- 判断が必要だった理由: βの画面変更は本番へそのまま採用しない一方、人間の指摘、操作上の改善、PC・モバイル検証まで捨てると同じ問題を再発させる。
- 選択肢: 画面判断をすべて破棄する／本番仕様へ混ぜる／非拘束の画面設計資料として分離する。
- 決定: 非拘束の画面設計資料として分離する。機能・データの採用判断は`PRODUCTION_CONTEXT.md`、βのレイアウト案・観察・未解決の比較点は`BETA_SCREEN_DESIGN_REFERENCE.md`を参照する。
- 根拠: βの見た目を暗黙の完成形にせず、実利用で得た情報階層、状態表示、アクセシビリティ、レスポンシブ検証の知見を次の画面設計へ利用できる。
- 影響: P1を完了し、P2では統合コンテキストを制約、画面リファレンスを比較材料として扱う。βの色、寸法、カード、タブ数、ナビ配置、コンポーネントを自動継承しない。
- 再検討する条件: 人間レビューでβ案の特定要素を本番画面へ採用すると明示した場合。その要素だけを新しい画面判断として記録する。
- 参照: `BETA_CHANGE_AUDIT.md`、`BETA_SCREEN_DESIGN_REFERENCE.md`、`PRODUCTION_CONTEXT.md`、本人の本タスクでの明示方針。

## D-20260904-053 — ソリッド幾何学紋章を講習会バッジalphaとして採用する

- 状態: decided（Lecture完了から獲得する部分はD-20260904-060、seedに関する部分はD-20260905-082でsuperseded。視覚生成規則は継続）
- 判断が必要だった理由: 講習会完了からバッジを導出することは決まっていたが、バッジ画像の生成方法と、添付されたショーケースのどこまでを本番へ持ち込むかが未決だった。
- 選択肢: 添付HTMLを画面ごと採用する／バッジ生成規則だけを採用する／新しい表現を設計し直す。
- 決定: 添付HTMLのソリッド幾何学紋章を`badge alpha`として採用し、生成規則を`BADGE_ALPHA_SPEC.md`へ固定する。外周ベゼル、中間図形、反転アクセント、中心コア、6構図、4種類の対称数、8パレット、決定論的乱数を継承する。本番のseedは講習会名ではなく`badge-alpha-v1:${lectureId}`とする。プロフィールの配置や詳細UIなど、画面に関する判断はP2で別途行う。
- 根拠: 視覚的な多様性と同一Lectureでの再現性を両立できる。安定IDをseedにすれば、同名Lectureを区別でき、Lecture名の変更でも獲得済みバッジの外観を維持できる。生成結果を完了記録から再構成できるため、バッジ獲得状態の重複保存も不要である。
- 影響: `PRODUCTION_CONTEXT.md`のバッジ生成方法を確定し、参照HTMLを`references/badge-alpha.html`へ同一ハッシュで保存する。Tailwind CSS CDN、Lucide CDN、Google Fonts、サンプルデータ、ショーケースのレイアウト、デバッグ表示、クリック可能な`div`やフォーカス管理のないモーダルは採用しない。モーションは低減設定へ従う任意効果、SVG・PNG保存は画面設計で初回範囲を決める。
- 再検討する条件: 新しいバッジ生成器を追加する、Lecture IDの寿命や移行方式が変わる、修了証明としての真正性が必要になる、ロードマップ達成バッジへ生成系を拡張する場合。
- 参照: `BADGE_ALPHA_SPEC.md`、`references/badge-alpha.html`、本人の「これをバッジ alpha として採用したい」という明示判断。

## D-20260904-054 — FlowClassと適用時スナップショットFlowの2段構成にする

- 状態: decided（2段構成とsnapshotは継続。回答・進捗の型はD-20260905-087でsuperseded）
- 判断が必要だった理由: 再利用可能な案内資産、対象への適用、版履歴を3層で管理する案は、版管理が主要機能ではない初期リリースに対して複雑だった。一方、Stock上の原本を更新しても、適用済みのLecture / Sessionで使っている内容を意図せず変えない必要がある。
- 選択肢: `FlowDefinition / FlowVersion / FlowRun`の3層にする／Stockの`FlowClass`と対象へ適用した`Flow`の2段にする／FlowClassを直接対象から参照して常に最新版を使う。
- 決定: 2段構成にする。`FlowClass`は`id`、`type`、`text`を持つStock上の原本とし、対象を参照しない。Lecture / Sessionへ適用するときに`Flow`を作り、`flowClassId`、`targetId`、その時点の`FlowClass.text`のコピー、回答・進捗を保持する。`FlowClass.text`の後続更新は既存Flowへ伝播させない。`FlowClass.type`は対象制約でもあるため、Flowから参照された後は変更不可とし、別typeには新しいFlowClassを作る。
- 根拠: 2テーブルで再利用と適用済み内容の固定を両立できる。Flow自身の`text`が適用時スナップショットになるため、過去内容を守る目的だけの明示的な版テーブルは不要である。
- 影響: D-20260904-050の3属性は`FlowClass.type`へ移る。`lecture_pre`と`lecture_post`から作るFlowはLecture、`session_main`から作るFlowはSessionを対象とする。回答・進捗の具体型、Stockへの掲載・利用停止、FlowとLecture登録項目の接続は引き続きP0仕様として決める。
- 再検討する条件: Stock側で版履歴、版間差分、特定版の選択・再利用が主要な利用場面として必要になった場合。その時点で`FlowClassVersion`を追加する。
- 参照: 本人が提示した`FlowClass` / `Flow`モデルと、2026-09-04の明示方針。

## D-20260904-055 — 再放送を通常導線とLecture完了操作から外す

- 状態: decided（完了をLecture単位にした部分はD-20260904-060でsuperseded。再放送を通常導線と完了操作から外す判断は継続）
- 判断が必要だった理由: 完了はLecture単位である一方、複数Sessionをまとめる再放送から完了を記録すると、元Sessionをどこまで完了したかという未設計の対応関係が必要になる。
- 選択肢: 再放送でもLecture完了を許可する／再放送では完了操作を提供しない。
- 決定: 後者。`replayOfSessionIds`が1件以上あるSessionには受講完了操作を置かない。再放送は検索結果と通常のSession選択導線へ出さず、publishedであればSessionを指定するURLから詳細を閲覧できる。流入元やRefererによって表示を変えない。
- 根拠: 再放送と元Sessionの完了換算を初期リリースへ持ち込まず、URL直アクセスという限定的な閲覧要件を一貫したルールで満たせる。
- 影響: 通常Sessionの詳細からだけLecture単位の自己申告完了を記録する。URLの秘匿はアクセス制御として扱わない。後からSessionが追加された場合の既存完了は別途決める。
- 再検討する条件: Session別完了、総集編から元Sessionへの完了換算、再放送を通常探索へ含める必要が生じた場合。
- 参照: 本人の2026-09-04の明示方針。

## D-20260904-056 — draft Sessionの公開昇格に追加必須条件を設けない

- 状態: decided
- 判断が必要だった理由: 日時やResourceがoptionalである一方、published時に「必要に応じて」必須とする記述ではAPI検証が一意に決まらなかった。
- 選択肢: 日時・場所・Resourceなどを公開条件にする／schemaとして有効なら追加条件なしで公開できるようにする。
- 決定: 後者。schemaとして有効なdraft Sessionは、日時・場所・Resourceなどの追加必須条件なしでpublishedへ変更できる。
- 根拠: 日時未定や過去教材中心の公開を妨げず、初期リリースの公開規則を単純にできる。
- 影響: published Sessionを1件持てば、情報量が少ないLectureも公開対象になり得る。空文字、最大長、URLなどschema自体の入力検証は別途定義する。
- 再検討する条件: 内部試用で情報不足の公開が探索品質を損なうと確認された場合。
- 参照: 本人の2026-09-04の明示方針。

## D-20260904-057 — 初期ロードマップを一本道にする

- 状態: decided
- 判断が必要だった理由: 段階と選択肢を表現する要求はあったが、分岐条件と進捗計算が初期リリースに対して複雑だった。
- 選択肢: 必須・選択・分岐を持つ／段階とLectureを順に進む一本道にする。
- 決定: 後者。段階名と説明は保持し、段階内のLectureも順序付ける。すべてのLectureを必須とし、分岐、選択肢、「N件中M件」は初期範囲に含めない。同じLectureを同一ロードマップへ重複配置しない。
- 根拠: 学ぶ順番と理由を示す中心価値を維持しながら、進捗計算を一意にできる。
- 影響: 次は先頭から最初の未完了Lecture、全体完了は全Lecture完了として導出する。`PRODUCTION_CONTEXT.md`の選択肢要件をこの決定で置換する。
- 再検討する条件: 一本道では表せない具体的な学習経路が内部試用で確認された場合。
- 参照: 本人の2026-09-04の明示方針。

## D-20260904-058 — 初期リリースは認証済み部員全員へ編集を許可する

- 状態: decided
- 判断が必要だった理由: 役割名は定義されていたが、作成・編集・公開のAPI権限が未決だった。
- 選択肢: 所有者・共同編集者制を先に作る／認証済みtraP部員全員へ編集を許可する。
- 決定: 後者。認証済み部員は全員、Lecture、Session、FlowClass、Flow、Roadmapを作成・編集し、公開状態を変更できる。完了記録の追加・取消は本人だけに限定する。
- 根拠: 少人数の内部試用で運営知識を共同整備する段階では、所有権管理を主要導線へ持ち込まずに済む。
- 影響: 画面とAPIで同じ規則を守る。編集者IDや変更履歴を保存するかは個人データの扱いを含むため、実データ投入前に別途判断する。
- 再検討する条件: 誤編集、管理責任、引き継ぎの問題が生じる、または利用対象を広げる場合。
- 参照: 本人の2026-09-04の明示方針。

## D-20260904-059 — Flowを物理削除しない

- 状態: decided（削除禁止はD-20260904-062へ拡張して継続。回答・完了状態を保持する理由はD-20260905-087でsuperseded）
- 判断が必要だった理由: Flowは適用時のtext、回答、進捗を持つため、削除すると実行記録と適用時スナップショットを失う。
- 選択肢: Flowを削除可能にする／Flowの物理削除を禁止する。
- 決定: 後者。初期リリースではFlowの物理削除を提供しない。
- 根拠: Stock更新から独立した適用済み内容と、途中・完了後の記録を保持できる。
- 影響: βに存在するFlow削除API・画面は本番へ移植しない。誤作成、中断、利用終了を表す状態と、FlowClassの削除・利用停止は別途決める。
- 再検討する条件: 法的・個人データ上の消去要求、保存期間、管理上の削除要件が定まった場合。
- 参照: 本人の2026-09-04の明示方針。

## D-20260904-060 — 受講完了をSession単位で記録する

- 状態: decided
- 判断が必要だった理由: これまでLecture単位の完了を仮定していたが、複数Sessionを持つLectureで、どの回を学んだかを保持できなかった。
- 選択肢: Lecture単位の完了を保存する／Session単位の完了を保存し、Lecture相当の進捗を導出する。
- 決定: 後者。完了記録は`userId`、`sessionId`、`completedAt`を持ち、同じ利用者・Sessionの有効な完了を1件にする。通常Sessionの詳細から本人が追加・取消できる。再放送Sessionには完了操作を置かず、完了記録を作らない。
- 根拠: 実際に学んだ開催を失わず、複数Sessionの進捗を表現できる。バッジやロードマップの状態はSession完了から導出できる。
- 影響: D-20260902-013とD-20260904-055のLecture単位完了を置換する。Lecture相当の完了、講習会バッジ獲得、ロードマップ項目完了の集約条件はD-20260904-067で確定した。
- 再検討する条件: 再放送の完了換算、運営による一括記録、外部参加情報との連携が必要になった場合。
- 参照: 本人の「完了はSession単位」という2026-09-04の明示判断。

## D-20260904-061 — コンテンツ編集を属性更新イベントとして記録する

- 状態: decided（属性イベントは継続。Flowイベント属性と履歴表示はD-20260905-087、088で更新）
- 判断が必要だった理由: 認証済み部員全員が編集できるため、現在値だけを上書きすると、誰が何を変えたかと変更前内容を後から復元できない。
- 選択肢: 現在値だけを保存する／現在値に加えて属性ごとの更新イベントを保存する／イベント再生を正本とするevent sourcingを採用する。
- 決定: 2番目。Lecture、Session、FlowClass、Flow、Roadmapの更新ごとに、対象、属性path、変更前後のJSON値、編集者、時刻、changeSetを記録する。現在値とイベントは同じtransactionで保存し、現在値を正本とする。
- 根拠: 通常の読取と実装を単純に保ちながら、属性単位の変更確認と復旧材料を残せる。一保存内の複数変更はchangeSetでまとめられる。
- 影響: Flowから対象属性を変更する場合も通常編集と同じ更新サービスを通す。Flow実行状態の扱いはD-20260904-066で確定し、Flow自身の属性更新イベントとして記録する。`actorId`にはNeoShowcaseから受け取るtraQ IDを保存する。
- 再検討する条件: 完全な状態復元、差分承認、履歴からの自動undo、法的な保持・消去要件が必要になった場合。
- 参照: 本人の「属性ごとの更新イベントを記録する」という2026-09-04の明示判断。

## D-20260904-062 — 初期リリースでは主要コンテンツを削除しない

- 状態: decided
- 判断が必要だった理由: Lecture、Session、FlowClass、Flow、Roadmapは相互参照され、物理削除すると学習履歴、適用時Flow、ロードマップの意味を失う。
- 選択肢: 対象ごとに物理削除を実装する／初期リリースでは削除機能を実装しない。
- 決定: 後者。Lecture、Session、FlowClass、Flow、Roadmapの削除APIと削除画面を本番の初期リリースへ入れない。誤りは編集またはdraftへの変更で訂正する。
- 根拠: 中心体験に削除は必要なく、参照整合性と履歴を安全側に保てる。
- 影響: D-20260904-059のFlow削除禁止を主要コンテンツ全体へ拡張する。βのLecture、Session、Flow、Roadmap削除実装は本番へ移植しない。完了取消は本人の学習記録操作として維持する。利用停止・アーカイブが必要になった場合は別状態として設計する。
- 再検討する条件: 誤作成の整理、利用停止、法的・個人データ上の消去、保存期間が具体的な要件になった場合。
- 参照: 本人の「削除は基本的に実装しない」という2026-09-04の明示判断。

## D-20260904-063 — 実装技術はmainの技術選定を正本とする

- 状態: decided
- 判断が必要だった理由: 仕様文書のTypeScript・GraphQL風の型や、参照デモのGo Parserが、そのまま本番技術の指定または移植元と誤解される可能性があった。
- 選択肢: 仕様作成時に別の実装技術を選ぶ／β・参照デモの技術を継承する／mainの技術選定と既存アーキテクチャに従う。
- 決定: 3番目。現在作成している文書はドメイン、状態、制約、文法、受け入れ条件を定める。実装言語、フレームワーク、DB上の物理schema、データアクセス、migration、テスト構成はmainを技術上の正本とする。
- 根拠: 本番基盤と異なるβ・参照デモの制約を持ち込まず、mainの保守・運用規約へ自然に統合できる。
- 影響: 文書中のTypeScript・GraphQL風の型は擬似表現として扱う。`workshop-wizard-demo`のGo Parserとβコードは挙動の参照に限り、実装時に一括コピーしない。mainを確認したうえで同じ意味をその技術に適した形へ翻訳する。
- 再検討する条件: mainの技術構成自体を変更する明示判断が行われた場合。
- 参照: 本人の「mainの技術選定に従って実装させる」という2026-09-04の明示方針。

## D-20260904-064 — Go HTTPフレームワークにEcho v4を使う

- 状態: decided
- 判断が必要だった理由: mainはGoとOpenAPI strict serverを選んでいたが、HTTP実装は標準`net/http`の初期構成だった。
- 選択肢: 標準`net/http`を継続する／Echo v4へ統合する。
- 決定: 本人の明示指定に従いEcho v4を採用し、oapi-codegenの`echo-server`と`strict-server`を生成する。
- 根拠: mainのGo/OpenAPI構成を保ったまま、本人が指定した本番フレームワークへ合わせられる。
- 影響: middleware、router、handler登録はEchoへ統一する。OpenAPIとdomain/storeはフレームワークへ依存させない。
- 再検討する条件: Echoの保守終了、またはmain全体のHTTP基盤を変更する明示判断がある場合。
- 参照: 本人の「Go は Echoを使う」という2026-09-04の明示判断。

## D-20260904-065 — UUID、明示保存、楽観ロックを初期の更新基盤にする

- 状態: superseded by D-20260905-080 and D-20260905-088（FlowClass Stock編集とRoadmap編集の明示保存・revision比較、属性イベントは継続）
- 判断が必要だった理由: バッジseedと相互参照を安定させ、全員編集時の上書きを検知する必要があった。
- 選択肢: 連番IDと後勝ち更新／UUIDとrevision比較。
- 決定: 全entityへUUIDを発行し、更新時は`expectedRevision`を要求する。編集とFlowは明示保存し、不一致を409で拒否する。
- 根拠: IDを環境間で維持でき、同時編集で変更履歴を失う前に競合を検知できる。
- 影響: 更新成功ごとにrevisionを増やし、現在値と属性イベントを同じtransactionへ入れる。
- 再検討する条件: オフライン編集、差分マージ、ID統合が必要になった場合。

## D-20260904-066 — Flow formatVersion 1と実行規則を固定する

- 状態: superseded by D-20260905-087（ページ区切り、対象属性入力、値展開、copy/code、formatVersionは継続）
- 判断が必要だった理由: FlowClassのtextをコピーしても、文法と進捗keyが変われば過去Flowを再開できない。
- 決定: 各ページの`# `、`---`区切り、`{{ key }}`入力、`[[ key ]]`展開、`- [ ]{#stable-key}`タスク、copy/code fenceからなる限定文法をformatVersion 1とする。任意式は扱わない。
- 根拠: 運営手順、対象属性入力、コピー文、進捗を表せる最小構造で、安定keyにより編集位置の変化から進捗を守れる。
- 影響: 同じFlowClassは同じ対象へ1回だけ適用する。ページ移動で保存し、全タスク後の明示操作で完了する。完了後は読み取り専用とする。
- 再検討する条件: 条件分岐、入力型、includeが必要になった場合は新formatVersionを追加する。

## D-20260904-067 — 現在公開中の通常Session全件でLecture完了を導出する

- 状態: decided
- 判断が必要だった理由: バッジとRoadmapはLecture単位だが、保存する完了事実はSession単位である。
- 選択肢: 1件完了／全通常Session／全published通常Session。
- 決定: publishedな通常Sessionを1件以上持ち、その全件を本人が完了したときLectureを完了とする。
- 根拠: draft準備中の回と再放送を学習要件にせず、学習者に見えている必須回と一致する。
- 影響: 通常Sessionを追加公開するとLecture、バッジ、Roadmap進捗は未完了へ戻り、追加分完了後に再び完了する。バッジ獲得日は必要Sessionの最後の完了日時から導出する。
- 再検討する条件: 選択回、免除、版ごとの修了条件が必要になった場合。

## D-20260904-068 — Roadmapは段階を持つ現在値型の一本道にする

- 状態: superseded by D-20260905-091
- 判断が必要だった理由: 順序だけでなく段階の意味を保持しつつ、公開条件と変更後の進捗を一意にする必要があった。
- 決定: Roadmapはtitle、description、audience、published、安定IDを持つStage、順序付きLecture Itemを持つ。公開時は空Stage、重複Lecture、非公開Lectureを拒否し、進捗は現在値から再計算する。
- 根拠: 初期の一本道を単純に表現し、元のSession完了事実を失わず編集できる。
- 影響: 公開後の編集は同じRoadmapへ即時反映する。初期版ではRoadmap versionを持たない。
- 再検討する条件: 公開版の固定、分岐、選択科目、修了証が必要になった場合。

## D-20260904-069 — NeoShowcase認証とtraQ不変User IDを使う

- 状態: decided
- 判断が必要だった理由: 全員編集と本人完了をAPIで保証し、編集者を後から参照できる識別子が必要だった。
- 決定: 本番はNeoShowcaseの部員認証を通し、`X-Forwarded-User`のtraQ IDをbackendのdirectoryで不変User IDへ解決する。閲覧を含む全APIを認証対象にし、ローカルだけ`DEV_USER`を許可する。
- 根拠: mainの運用基盤に一致し、Bot tokenをfrontendへ渡さず本人操作とactorを確定できる。
- 影響: backendを認証なしの別ingressへ公開しない。User/Group一覧はbackendで世代単位にcacheする。
- 再検討する条件: 一般公開、退部者表示、複数組織認証が必要になった場合。

## D-20260904-070 — BasiQ UIの利用可能な固定版を限定採用する

- 状態: superseded by D-20260904-071
- 判断が必要だった理由: mainはBasiQ UI利用を予定していたが、最新betaは依存の公開経過時間制限により取得できなかった。
- 決定: MITライセンスとpostinstall内容を確認した`basiq-ui@0.1.0-beta.0`をexact versionで導入し、ButtonとThemeProviderを中心に使う。product layoutは通常CSSで実装する。
- 根拠: セキュリティ設定を緩めず、mainのUI方針を初期画面へ反映できる。
- 影響: `pnpm-workspace.yaml`で公開経過時間、exotic subdependency、exact saveを維持し、許可したbuild scriptを明示する。
- 再検討する条件: 制限を満たす安定版または互換betaが利用可能になった場合。

## D-20260904-071 — 確定画面設計をBasiQ UI beta.3で本番へ実装する

- 状態: decided（講習会編集で画面プロトタイプを仕様より優先した部分はD-20260904-072でsuperseded。BasiQ UIと他画面への適用は継続）
- 判断が必要だった理由: 本人が、別途設計した画面をBasiQ UIで本番実装へ忠実に反映する方針を明示した。D-20260904-070のbeta.0では設計に使われたTabs、FormField、Input、Textarea、Switch、Checkboxを利用できなかった。
- 選択肢: beta.0の限定コンポーネントで近似する／監査済みのbeta.3を固定して設計と同じコンポーネント構成を使う。
- 決定: 後者。リポジトリ内の監査済み`basiq-ui@0.1.0-beta.3` tarballをvendorとしてexact固定し、InterとM PLUS 1pも固定する。ホーム、講習会詳細・編集、開催詳細、Roadmap一覧・詳細・編集、プロフィール、運営、Flow Stock、Flow実行を共通ナビゲーションとBasiQ UI部品で統一する。
- 根拠: 画面設計で使われた部品とトークンをそのまま利用でき、外部取得の再現性にも依存しない。
- 影響: 画面プロトタイプをレイアウト、密度、レスポンシブ動作の正本として扱う。製品仕様を優先し、プロトタイプにあった削除操作は入れず、Flow、Session完了、Roadmap段階など本番データ構造へ接続する。
- 再検討する条件: BasiQ UIの安定版が公開され、同じ画面を互換性を保って移行できる場合。
- 参照: 本人の「basiQ UI 使って、設計した画面と全く同様の画面にして」という2026-09-04の明示依頼。

## D-20260904-072 — 講習会編集はLecture / Session / Flowの確定仕様を画面構造へ直接反映する

- 状態: superseded by D-20260904-073 and D-20260905-089（データ境界、構造化入力、公開状態の扱いは継続）
- 判断が必要だった理由: 画面プロトタイプを移植した講習会編集は、LectureとSessionの情報境界、Sessionから導出するLecture公開状態、3属性Flowの対象と時系列、構造化されたResourceとLectureRelation、保存後確認などを十分に表現できていなかった。
- 選択肢: 既存の「全般・開催・Flow・設定」タブを部分修正する／仕様を情報構造の正本として編集画面を組み直す。
- 決定: 後者。編集画面を「講習会情報」と「開催とFlow」の2面にする。講習会情報はLecture属性だけを扱い、開催とFlowは`lecture_pre`、順序付きSessionと各`session_main`、`lecture_post`を時系列に並べる。Lectureの公開状態は編集させず、published Session件数から導出した状態として表示する。
- 根拠: 利用者が保存先と公開への影響を取り違えず、確定済みデータ構造を画面から確認できる。Flowを別の管理タブへ分離するより、対象となるLecture / Sessionとの対応が明確になる。
- 影響: ResourceはtitleとURL、LectureRelationはtypeと対象Lecture、再放送は複数の元Sessionを選ぶ構造化入力にする。Session内容の複製と再放送としての複製を用意し、複製後は独立した新規Sessionとして保存する。URL queryの`session`から対象編集を開き、初期読込失敗時は空フォームを表示しない。保存後はpublished Session数とLectureの表示状態を確認してから編集へ戻る。
- 再検討する条件: 内部試用で2面構成よりSessionごとの専用URLや別画面が明確に理解しやすいと分かった場合。
- 参照: 本人の「仕様が反映されてないので、仕様を見直した上でもう一度実装したい」という2026-09-04の明示依頼。

## D-20260904-073 — 講習会編集の横タブをFlow単位にする

- 状態: superseded by D-20260904-074 and D-20260905-086（1 Flow 1タブだけ継続）
- 判断が必要だった理由: D-20260904-072の2面構成では、画面設計における横タブとFlowの対応が失われ、各Flowを独立した作業単位として認識しにくかった。
- 選択肢: Lecture情報と開催構造の2タブ／適用されたFlowごとの横タブ。
- 決定: 横タブ1つを適用済みFlow 1つに対応させる。`lecture_pre`を「全般」、各Sessionの`session_main`を開催名、`lecture_post`を「事後」として時系列順に並べる。未適用の対象にはFlowClassを選ぶプレースホルダータブを置く。開催追加と設定はFlowではない補助タブとして末尾へ置く。
- 根拠: 確定済みのFlow対象分類を保ちながら、画面設計の「全般・各開催を横タブで切り替える」操作をそのまま使える。
- 影響: 同じ対象へ複数Flowが適用済みの場合もFlowごとに別タブを作る。開催タブには対応するメインFlowとSession概要を表示し、Session属性編集も同じタブ内で開く。設定タブにはLecture属性と開催一覧を置く。
- 再検討する条件: Flow数が横幅を恒常的に超える、または1対象1Flowを制約として確定する場合。
- 参照: 本人の「Flowは横タブの1つずつに対応するので、タブの部分は画面設計を参考にして実装して」という2026-09-04の明示依頼。

## D-20260904-074 — Flowタブの中でFlowを直接実行する

- 状態: decided（タブ内Rendererは継続。task・明示保存・完了操作はD-20260905-087でsuperseded）
- 判断が必要だった理由: Flowタブが進捗概要と別画面へのリンクだけを表示しており、タブ自体がFlowの作業面になっていなかった。
- 選択肢: Flow概要を表示して専用画面へ遷移／タブ内へFlow Rendererを表示。
- 決定: 適用済みFlowのタブ内容はFlow本文、ページ進捗、対象属性入力、task、copy/code、前後移動、途中保存、完了操作を持つFlow実行画面そのものにする。Flow未適用のプレースホルダーだけはFlowClass選択を表示し、適用後は画面遷移せず同じタブでFlowを開始する。
- 根拠: 横タブとFlowの1対1対応が見た目だけでなく操作単位としても成立し、画面設計のウィザード領域を現在のFlow概念へ直接対応づけられる。
- 影響: Session概要や編集フォームはFlowタブから除き、設定タブへ集約する。Flow内の入力がLecture / Session属性を更新した場合は編集画面の現在値とrevisionも再読込する。
- 再検討する条件: 同じRendererを利用する画面が増え、状態管理をさらに共通化する必要が生じた場合。
- 参照: 本人の「タブの中身はFlowそのものにして」という2026-09-04の明示依頼。

## D-20260904-075 — 固定commitの静的画面を本番表示層の正本にする

- 状態: decided
- 判断が必要だった理由: 本番画面へ機能を接続した過程で、確定済みの静的画面から共通シェル、余白、幅、情報密度が離れていた。
- 選択肢: 現行画面を微調整する／固定commitのDOMとCSSを表示層へ移し、既存機能をその見た目へ接続する。
- 決定: `references/ui-canonical/screens.json`が指す固定commitとpathを画面ごとの視覚的正本とし、後者を採用する。共通シェルは`home-and-shell`、ホームの探索内容はより具体的な`discovery`を優先する。API、router、認証、永続化、入力、エラー処理は維持する。
- 根拠: 画面設計で確定したDOM階層、寸法、色、書体、レスポンシブ挙動を、その後の実装解釈で変えずに本番機能へ接続できる。
- 影響: 静的画面にない本番機能は同じカード、入力、ナビゲーションの言語で補う。各画面を1440×900と390×844で比較し、横方向のoverflowとconsole警告・エラーを確認する。
- 再検討する条件: 本人が新しい画面設計を正本として明示した場合。
- 参照: `references/ui-canonical/README.md`、`references/ui-canonical/screens.json`。

## D-20260904-076 — Profile正本の欠落pathは同commitの実画面で補う

- 状態: superseded by D-20260904-077
- 判断が必要だった理由: `screens.json`がProfile正本として示す`app/prototypes/profile`は、指定commit `8b41ea6b43b7f532bf77d7964fa6d58997fb35c0`に存在しない。
- 選択肢: Profileの再統合を止める／同じ固定commitの`app/ui/views/ProfileView.ts`と`app/styles/pages.css`、`app/qa/redesign-profile-desktop.jpg`を使う。
- 決定: 後者。同commit内で実際に使われているProfileのDOM、CSS、QA画像を視覚的正本として扱う。
- 根拠: commit境界を維持したまま、ヘッダー統計、3タブ、バッジ一覧＋詳細欄、完了一覧、Roadmap進捗という確定構造を復元できる。
- 影響: 本番のSession単位完了とbadge alpha SVGは維持し、正本の情報配置へ接続する。正本インデックス自体は履歴として書き換えない。
- 再検討する条件: Profileの正しい静的prototype pathまたは新しい固定commitが提示された場合。
- 参照: `references/ui-canonical/screens.json`。

## D-20260904-077 — Profileは新しい固定commitと実データ3指標を統合する

- 状態: decided（seedに関する部分だけD-20260905-082でsuperseded）
- 判断が必要だった理由: Profileの正しい静的prototypeとbadge生成器を含む固定commitが提示され、同時に本番で算出している3つの数値サマリーを残す明示指定があった。
- 選択肢: D-20260904-076の代替正本を継続する／新しい`app/prototypes/profile`を正本にして本番3指標だけを統合する。
- 決定: 後者。`574c7ec5925345ada1c67094f502f16a3a5901ab`のProfileを表示の正本とし、`完了した講習会`、`完了した開催`、`ロードマップ内の完了`は現在のAPIデータから算出する横並び構造をProfileヘッダーへ統合する。バッジは同commitの`badge-generator.ts`と同じアルゴリズムとPRNG呼び出し順を使い、seedは既存仕様の`badge-alpha-v1:${lectureId}`を維持する。
- 根拠: 確定した最新画面とバッジ生成規則を採用しつつ、利用者が気に入った実データの学習集計を失わない。
- 影響: 以前の代替正本判断D-20260904-076をsupersededとする。Profileのタブ、バッジ一覧と詳細、完了Session、Roadmap進捗は本番APIへ接続したまま正本の視覚言語へ合わせる。生成SVGへユーザー入力を連結しない。
- 再検討する条件: 本人が別のProfile正本または数値サマリー構造を明示した場合。
- 参照: `references/ui-canonical/README.md`、`references/ui-canonical/screens.json`、本人の2026-09-04の訂正指示。

## D-20260904-078 — Flow追加とSession追加を別の導線にする

- 状態: superseded by D-20260905-086（D-20260904-073、D-20260904-074の未適用プレースホルダーに関する部分をsuperseded。1 Flow 1タブとタブ内実行は継続）
- 判断が必要だった理由: Flowが存在しない対象まで横タブにすると、タブとFlowの1対1対応が崩れ、Flow追加と開催追加も同じタブ列で混在していた。
- 選択肢: 対象ごとの未適用プレースホルダータブを残す／適用済みFlowだけをタブにし、追加操作をモーダルへ分離する。
- 決定: 後者。横タブの内容は適用済みFlowそのものだけにする。`＋ Flowを追加`からモーダルを開き、Lecture事前、各Sessionメイン、Lecture事後の対象を選ぶと、属性が一致し未適用のFlowClassだけを表示する。適用後は画面遷移せず、新しいFlowタブを選択して実行を開始する。Session追加は設定タブの開催一覧から行う。
- 根拠: タブの意味をFlow実体へ限定しながら、3属性と対象の対応を追加時に明示できる。
- 影響: Flow未適用の対象はタブ列に現れない。適用可能なFlowClassがない対象ではFlow Stockへの導線を表示する。適用済みFlowが0件の場合は設定タブを初期表示する。
- 再検討する条件: FlowClassの候補数が増え、モーダル内で検索やカテゴリ絞り込みが必要になった場合。
- 参照: 本人の「Flowは横タブの1つずつに対応する」「タブの中身はFlowそのものにして」という2026-09-04の明示依頼。

## D-20260904-079 — 発見カードのhoverをフラットに保つ

- 状態: decided（D-20260904-075の静的UI正本に対する最新の上書き）
- 判断が必要だった理由: 正本の再統合で講習会カードへ影と上方向の移動が戻り、本人が不要と明示したフラットな操作表現から外れていた。
- 決定: ホームの講習会・RoadmapカードとRoadmap一覧カードは、hover時に枠色だけを変える。`box-shadow`と`translateY`は使わない。キーボード操作ではaccent色の2px outlineを維持する。
- 根拠: マウス操作時の浮き上がりをなくしつつ、hoverとfocus-visibleの双方を視認できる。
- 影響: `HomeView.vue`と`RoadmapListView.vue`で共通の`discovery-card`表現を揃える。データ、API、レイアウト寸法は変更しない。
- 再検討する条件: カードの状態表現をdesign tokenまたは共通コンポーネントへ集約する場合。
- 参照: 本人の2026-09-04の明示依頼。

## D-20260905-080 — Lecture / Sessionの内部IDをAUTO_INCREMENT BIGINTへ限定する

- 状態: decided（連番IDは継続。楽観ロックはD-20260905-088でsuperseded）
- 判断が必要だった理由: Lecture詳細URLで人が扱える短い連番を使い、SessionをLecture内の回として扱う仕様が確定した。一方、既存の全entity UUID方針はLecture / SessionにもUUIDを要求していた。
- 選択肢: UUIDと公開用連番を併存させる／LectureとSessionの主キー自体をMariaDBの`AUTO_INCREMENT BIGINT UNSIGNED`へ置き換える。
- 決定: 後者。Lecture / Sessionだけを連番主キーへ変更し、APIとJSONではJavaScriptの整数精度に依存しない10進文字列として表す。FlowClass、Flow、Roadmap、更新イベントなどのIDと外部User IDは従来どおりUUIDまたは外部IDを使う。開発DBは移行せず空にしてmigrationから作り直す。
- 根拠: 二重IDを持たずURLを`/lectures/<integer>#<round>`にでき、既存のFlow IDや外部IDの意味も変えずに済む。
- 影響: fixtureもIDを指定せずDBへ採番させる。各Lectureの同じ`order`には通常Sessionを1件だけ許し、同じ回の再放送Sessionは同じ`order`を持つ。楽観ロックと属性単位イベントは継続する。
- 再検討する条件: 複数DBで独立採番したデータの統合、オフライン作成、または外部公開IDの秘匿が必要になった場合。
- 参照: 本人の2026-09-05のLecture / Session連番化、開発DB初期化、二重IDを持たないという明示判断。

## D-20260905-081 — 学習者向けSession詳細をLecture詳細へ統合する

- 状態: decided（単発LectureのタブはD-20260905-083、再放送表示とfragment形式はD-20260905-085でsuperseded）
- 判断が必要だった理由: Session内部IDをURLへ出さず、同じ回の通常開催と再放送を一緒に見せる詳細導線が必要になった。
- 選択肢: Session詳細URLを残してLectureから遷移する／学習者向けSession詳細を廃止しLecture詳細へ統合する。
- 決定: 後者。Lecture詳細URLを`/lectures/<integer>#<round>`とし、公開Sessionを`order`でまとめた回ごとの横タブを表示する。同じorderの通常開催と再放送は同じタブ内に並べ、日時、場所、説明、教材を表示する。完了操作は通常Sessionだけに対応する。
- 根拠: 学習者に必要な単位はLecture内の第何回かであり、内部Session IDを知る必要がない。fragmentなら回切替を履歴へ残し、直接表示と再読込にも対応できる。
- 影響: `/sessions/:id`の学習者ルートとリンクを削除する。Session取得・更新APIと運営編集は維持する。プロフィールの完了記録はLecture IDと回番号を返し、該当fragmentへ遷移する。
- 再検討する条件: Session単体をLecture外から公開・共有する要件が確定した場合。
- 参照: 本人の2026-09-05のLecture詳細URL、同一回グループ、Session詳細統合という明示判断。

## D-20260905-082 — badge alphaのseedをLecture名にする

- 状態: decided
- 判断が必要だった理由: 同じ講習会を年度ごとに別Lectureとして作っても、同じ種類のバッジとして同一デザインを使う仕様が確定した。
- 選択肢: 内部Lecture IDをseedにする／Lecture名をseedにする。
- 決定: `badge-alpha-v1:${lectureName}`をseedにする。グループ情報は加えず、固定済みの生成アルゴリズムと疑似乱数の呼び出し順は変更しない。
- 根拠: 同名講習会は年度とレコードが異なっても同じバッジになり、名前が異なる講習会は別デザインになる。
- 影響: Lecture名を変更すると表示されるバッジデザインも変わる。過去の生成画像を保存する版管理は初期範囲に加えない。
- 再検討する条件: 改名後も過去デザインを固定する要件、講習会の恒久的な系列ID、または版履歴が必要になった場合。
- 参照: 本人の2026-09-05のbadge seedはLecture名、同名は同デザイン、グループ情報を加えないという明示判断。

## D-20260905-083 — 回の選択肢があるLectureだけ横タブとfragmentを表示する

- 状態: decided（BasiqTabs content slotはD-20260905-084、fragment形式はD-20260905-085で更新）
- 判断が必要だった理由: 通常開催と録画を同じroundにまとめた結果、論理的な開催回が1回だけのLectureにも選択肢のない「第1回」タブと`#1`が表示されていた。
- 選択肢: 1回でも常にタブと`#1`を表示する／2回以上のLectureだけに表示する。
- 決定: 後者。公開Sessionを`order`でまとめたroundが2件以上のときだけ横タブを表示し、`#<round>`を選択状態として使う。roundが0件または1件ならfragmentを付けず、`#1`などで入った場合も同じLectureのfragmentなしURLへ置き換える。
- 根拠: 選べないタブを除き、単発Lectureの正規URLを短くできる。複数回Lectureの直接表示、再読込、戻る・進むは維持できる。
- 影響: 同じ`order`の通常Sessionと録画Sessionは1 roundのままなので、録画追加だけではタブが増えない。BasiqTabsのcontent slot構造は別途のコンポーネント監査が終わるまで変更しない。
- 再検討する条件: 単発Lectureでもround名や進行状態をタブとして常時示す画面要件が確定した場合。
- 参照: 本人の2026-09-05の単発タブ非表示と`/lectures/1`正規化の明示判断。

## D-20260905-084 — 複数回Lectureの回本文をBasiqTabsのtabpanel内へ置く

- 状態: decided
- 判断が必要だった理由: BasiqTabsのcontent slotへ空要素だけを渡していたため、`aria-controls`先のtabpanelが空で、選択回の本文がタブ構造の外に置かれていた。
- 選択肢: 空要素だけを削除して本文を外に残す／各itemに対応する実際の回本文をcontent slotへ渡す。
- 決定: 後者。回本文を`LectureRoundDetail`へ抽出し、複数回では選択中のtabpanelへ対応roundの本文を配置する。タブ切替時はBasiqTabsの既定動作に従って本文を切り替える。単発ではBasiqTabs自体を描画せず、同じコンポーネントを直接表示する。
- 根拠: tab、`aria-controls`、tabpanel、回本文の意味上の対応が成立し、単発と複数回で本文実装も重複しない。
- 影響: BasiqTabs内部classには依存せず、この画面の`[role="tabpanel"]`だけwidth 100%・左右margin 0・padding 0・白背景にし、タブ直下24px、PCの本文＋310px補助欄、モバイルの本文先行を維持する。
- 再検討する条件: BasiqTabsが複雑なVNodeをItems APIの`content`として型安全に受け取れるようになる、またはtabpanelの既定余白が画面設計と一致する場合。
- 参照: basiQ UI公式Storybookとインストール済みbeta.3 bundleの監査結果、および本人の2026-09-05の具体指示。

## D-20260905-085 — 学習者向けLecture詳細を通常開催の学習内容中心にする

- 状態: decided
- 判断が必要だった理由: 学習者が後から教材と学習内容を探す画面で再放送の開催履歴が本文を押し下げ、数字だけのfragmentと表示中の「第N回」も一致していなかった。
- 選択肢: APIが返す通常開催と再放送を同じ回に表示し続ける／学習者向けLecture詳細だけを通常開催に限定する。
- 決定: 後者。通常Sessionだけを`order`順に回へ写像し、回数・タブ・本文・学習状況を構成する。節見出しは「この回で学べること」とし、SessionとLectureの教材はbookアイコン付きのbasiQ UI `Button`で安全に別タブへ開く。複数回の正規fragmentはタブ表示と同じ`#第N回`、単発はfragmentなしとする。旧`#N`は対応する新fragmentへ置換する。
- 根拠: 学習者が内容、教材、対象者、前提知識へ早く到達でき、タブ名と共有URLから同じ回を読み取れる。再放送の管理情報を失わず表示責務だけを分離できる。
- 影響: Lecture API、DB、運営編集、再放送作成は維持する。再放送しかない`order`は学習者向け回数へ含めない。プロフィールの完了Sessionリンクも`#第N回`へ統一する。`LectureRoundDetail`と実tabpanel構造は継続する。
- 再検討する条件: 学習者向け画面で再放送固有の教材を探す要件、または通常開催とは別の公開アーカイブ画面が確定した場合。
- 参照: 本人の2026-09-05の学習内容中心、再放送非表示、日本語fragment、basiQ教材ボタンという明示判断。

## D-20260905-086 — 対象ごとにFlowを1件持ち、講習会作成を原子的に行う

- 状態: decided
- 判断が必要だった理由: 任意にFlowを追加する現行モデルでは、講習会・各開催・事後の必須作業面が欠けたり、同じ対象へ複数Flowが並んだりする。講習会だけを先に作る方式も、作成直後の編集タブ構造と一致しない。
- 選択肢: Flowを任意追加のままにする／対象属性ごとに1件を必須にする。
- 決定: Lectureは`lecture_pre`と`lecture_post`のFlowを各1件、Sessionは`session_main`を1件、常に持つ。講習会作成時に講習会名と3種類のFlowClassを選び、Lecture、第1回draft Session、3件のFlowを同じtransactionで作る。第1回の初期名は「第1回」、orderは0とする。
- 根拠: 画面の対象タブと保存対象が常に1対1になり、作成直後から事前、開催、事後の運営手順を欠落なく使える。
- 影響: タブは`講習会`、順序付きの`第N回`、開催追加`+`、`事後`、`一覧編集`、`…`とする。`+`は空または複製によるSession追加で、Flow追加には使わない。複製したSessionのFlowは元Flow状態をコピーせず、元Flowが参照するFlowClassの現在本文から作る。
- 再検討する条件: Flowを持たない対象や、同じ対象で複数の並行手順を持つ具体例が必要になった場合。
- 参照: 本人の2026-09-05の講習会編集に関する壁打ち結果と実装承認。

## D-20260905-087 — Flow実行状態を本文とページ位置へ縮小する

- 状態: decided
- 判断が必要だった理由: Flow固有answers、task安定key、完了状態、完了後read-onlyは、Lecture / Session属性を正本とする初期用途に対して状態を重複させていた。
- 選択肢: 独立した実行状態を維持する／チェックを本文へ含め、ページ位置だけを別に持つ。
- 決定: 後者。Flowは`flowClassId`、対象、適用時`text`、`formatVersion`、`currentPage`を持つ。`---`ごとに1ページを表示する。チェックはkeyなしの`- [ ]` / `- [x]`をFlow.text内で直接変更し、frontendは全文を送らずbackendのチェック専用操作が現在本文を解析して該当markerだけを更新する。複合属性は`{{ edit path }}`で一覧編集と共通の中央モーダルを開く。
- 根拠: 対象属性、チェック、ページ位置の正本が一意になり、FlowClass snapshotの単純さを保てる。
- 影響: `answers`、`tasks`、`active/completed/cancelled`、完了条件、完了後read-only、タスク安定keyを廃止する。使用FlowClass変更時は対象属性を維持し、同じFlowのsnapshot、チェック、ページ位置を現在FlowClassと先頭ページへ置き換える。
- 再検討する条件: Flowだけに属する回答、業務完了の証跡、本文編集に耐えるチェック同定が必要になった場合。
- 参照: 本人の2026-09-05のFlow形式に関する明示判断。

## D-20260905-088 — 属性単位の自動保存と後勝ち競合警告を採用する

- 状態: decided
- 判断が必要だった理由: Lecture / Session全体の明示保存とrevision不一致409では、別属性の同時編集まで拒否し、Flowと一覧編集の短い更新を失いやすい。
- 選択肢: Lecture / Session全体を楽観ロックで拒否する／Lecture / Session属性ごとに自動保存し、同属性競合も後勝ちで成功させる。
- 決定: 後者。Lecture / Sessionの単一属性は入力停止またはblur、配列・複合値はモーダル確定時に保存する。requestの`baseValue`とtransaction内の現在値を比較し、異なる場合も後勝ちで保存してresponseだけに競合を示す。競合フラグは永続化しない。現在値、revision増加、属性イベントを同じtransactionへ入れる。
- 根拠: 異なる属性の共同編集を妨げず、同属性の変更も履歴へ`A→B`、`B→C`として自然に残せる。
- 影響: Lecture / Session更新契約から`expectedRevision`とrevision競合409を除く。FlowClass Stock編集とRoadmap編集は今回の画面スコープ外であるため、現行の明示保存、`expectedRevision`、競合409を維持する。localStorageは未送信属性差分だけを保持し7日で失効する。server値がbaseValueと同じなら自動復元し、違えば自動適用せず確認、コピー、破棄だけを提供する。開催順の並べ替えは複合操作なので明示保存する。
- 再検討する条件: 承認制編集、厳密なロック、複数属性にまたがる利用者単位のマージが必要になった場合。
- 参照: 本人の2026-09-05の自動保存、競合、localStorageに関する明示判断。

## D-20260905-089 — 編集面の担当者・教材・公開・履歴・書き出しを確定する

- 状態: decided
- 判断が必要だった理由: 一覧編集の情報密度、担当者の多重指定、講義資料と関連リンクの混在、公開時の情報不足、履歴とJSONの操作範囲が未確定だった。
- 選択肢: 現行の複数配列と汎用Resourceを維持する／初期用途へ合わせて単純化する。
- 決定: Lectureの運営担当は個人1名またはグループ1件とし、グループはIDと選択時点の`groupName`を保存する。問い合わせ先は統合し独立属性を持たない。Session講師は個人0〜1名。Lecture / Sessionは`material={url,title?}`を最大1件持ち、title未設定時のUI表示は「講義資料」とする。その他のresourcesは双方が複数持て、knoQ URLはSessionだけが持つ。
- 根拠: 実際の責任主体と中心教材を明確にし、一覧編集を表形式へ肥大化させず属性単位で扱える。
- 影響: 一覧編集はPCの属性別横レーンとモバイル縦積みを使う。公開確認は推奨未設定項目と最後のpublished Sessionを戻す影響を警告するが、公開を妨げない。履歴はデータ変更とFlow操作を分けて閲覧・コピーだけ提供する。JSONは保存済みLecture / Session現在値を書き出すだけで入力しない。削除、archive、取り下げは実装しない。
- 再検討する条件: 複数運営担当、問い合わせ専任、複数の中心教材、履歴復旧、JSON importが必要になった場合。
- 参照: 本人の2026-09-05の講習会編集に関する明示判断。

## D-20260905-090 — Flow属性入力の記法を値の型で分ける

- 状態: decided
- 判断が必要だった理由: 単純値と配列・複合値に同じ入力記法を許すと、自動保存する入力欄と中央モーダルのどちらを出すかが本文だけでは決まらない。
- 選択肢: どちらの記法も全属性へ許可する／単純値は`{{ path }}`、配列・複合値は`{{ edit path }}`だけを許可する。
- 決定: 後者。FlowClassの作成・更新・適用時にbackend parserで検証し、誤った組み合わせは拒否する。
- 根拠: Flow本文からUIと保存単位を一意に決められ、古い不正なFlowClassも新しいFlowへ適用されない。
- 影響: frontendは単純値をタブ内の入力欄として自動保存し、`edit`を一覧編集と共通の中央モーダルへ接続する。
- 再検討する条件: 同じ属性へ複数の編集UIを選択可能にする具体的要件が生じた場合。
- 参照: `FLOW_FORMAT_ANALYSIS.md`とP7実装時のparser整合性確認。
---

## 追記テンプレート

## D-20260905-091 — Roadmapを段階なしのLecture／Session混在順序リストにする

- 状態: decided（D-20260904-057とD-20260904-068をsuperseded）
- 判断が必要だった理由: 最新の画面設計ではStageや項目メモを使わず、Lecture全体と特定のSessionを同じ一本道へ配置する。既存のStage JSONを単純に置換すると、旧データの複数Stageと項目を失う可能性があった。
- 選択肢: Stage配下のLecture Itemを維持する／`id`、`targetType`、`targetId`だけを持つ一次元Itemへ移行する。
- 決定: 後者。Roadmapは`title`、`description`、`audience`、`published`と順序付きItemを持つ。`targetType`は`lecture`または`session`とし、再放送Sessionは対象にしない。Lecture ItemはLecture完了、Session Itemは対象Session完了から進捗を導出する。
- 根拠: 一覧の各行が学習対象1件と直接対応し、画面、API、進捗の階層が一致する。特定回だけを学ぶ経路もLecture全体と同じリストで表現できる。
- 影響: nullableな`roadmaps.items`を追加し、`items IS NULL`の旧行だけは全Stageの全Lectureを保存順に平坦化して読む。旧`stages`列はdropも上書きもせず、移行後の更新では新`items`だけを保存する。公開Roadmapは1件以上のItemと公開済み通常対象を要求する。Roadmap削除は実装せず、編集画面ではItemをリストから外せる。
- 再検討する条件: 段階固有の説明、項目メモ、分岐、選択科目、または旧Stage構造を編集可能な形で復元する要件が確定した場合。
- 参照: 本人が指定した最終画面設計commit `bb023b0acad3da6b89423fa7fc618153f5b14614`、本人の2026-09-05の明示判断。

## D-20260905-092 — 横タブの内容をFlow作業面とし、年度引き継ぎは現在のFlowClassから再生成する

- 状態: decided
- 判断が必要だった理由: 対象タブの下に別の実行カードとページ進捗を重ねると、タブとFlowの1対1関係が画面上で弱くなる。また過年度Lectureを作り直す際、元Flowの実行状態までコピーすると新年度の作業開始状態にならない。
- 選択肢: タブ内に独立したFlowカードを置く／タブのtabpanel自体をFlowの目次と本文にする。過年度Flowを複製する／元Flowが参照する現在のFlowClassから新しいFlowを作る。
- 決定: 各`講習会`、`第N回`、`事後`タブのtabpanelを、その対象のFlow作業面そのものにする。Flow textの`---`区切りを目次へ写像し、選択ページの入力、チェック、コピー操作を本文に直接置く。年度引き継ぎではLectureの名前・説明・対象者・分野・0→1属性・運営担当、前提・後続関係、通常Sessionの順序・名前・説明、適用FlowClassの対応だけを引き継ぐ。新Lectureから元Lectureへの`previous_year`を1件作り、元の系譜はコピーしない。新Flowは参照FlowClassの現在本文から作成する。
- 根拠: タブ名、対象、保存先、表示中Flowが一致する。引き継ぎ後は新年度の運営作業を必ず未着手から始めつつ、Stockで改善済みの最新手順を利用できる。
- 影響: 新規作成画面は白紙作成と過去Lectureからの引き継ぎを選択できる。引き継ぎは単一transactionで、公開状態、日時・場所・knoQ、講師、教材・Resource、traQチャンネル、再放送、完了、チェック、ページ位置、履歴をコピーしない。通常Sessionがない、必須Flowがない、参照FlowClassが利用不能な場合は全体を失敗させる。
- 再検討する条件: 特定時点のFlowClass版を再利用する版履歴、元の回答を下書きとして移す要件、またはprevious_yearの複数元が必要になった場合。
- 参照: 本人の「Flowは横タブの1つずつに対応する」「タブの中身はFlowそのものにして」という明示依頼と、画面設計参照branch `origin/codex/leqtures-design-principles`。

## D-YYYYMMDD-NNN — 判断の題名

- 状態: decided | pending | superseded
- 判断が必要だった理由:
- 選択肢:
  1.
  2.
- 決定:
- 根拠:
- 影響:
- 再検討する条件:
- 参照:
